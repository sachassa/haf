# My Existing Project

> 사용자 소유 기존 파일 — 설치가 덮어쓰지 않아야 할 보호 대상(INV-3 시연용).
> 이 파일은 의도적 결함이 아니라 정상 보호 대상이다. Scaffold 설치·재설치·Uninstall이
> 이 파일을 보존해야 함을 시연하기 위한 사전 배치 사용자 파일이다 (12 §3.2-C CK-8, INV-3·INV-5).
>
> (시연 픽스처 — 실계약 문서·라이브 설정 표면 아님. 이 `new-project/`는 격리된 시연 설치본이며
>  라이브 `.claude/`·라이브 계약 문서가 아니다.)

이것은 신규/기존 프로젝트에 UAHF를 설치하기 전부터 존재하던 사용자 소유 README 파일이다.

특징적 사용자 내용 (보존 대조용 고정 문자열): USER-OWNED-CONTENT-v0.9-DEMO-PRESERVE-MARKER-7f3a.

Scaffold는 이 파일을 덮어쓰지 않는다. 재설치(멱등성) 시에도 무변경, Uninstall 시에도 보존된다.
