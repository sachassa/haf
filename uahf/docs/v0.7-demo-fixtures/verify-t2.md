시연 CP2 리포트 — v0.7 시연 wf-t2 (실계약 CP2 리포트 아님)

# verify-t2 — Task T2 CP2 검증 리포트 (cycle_id: v07-demo-t2)

프로토콜: framework/verifier/verifier-protocol.md 5단계 (입력 수령 → 기준 확인 → 항목별 판정 → 최종 판정 도출 → 리포트 산출). 스키마: framework/verifier/verification-report.md §2·§3·§4 (정본 06 §3.2-A/B/C). 시연 사이클용 경량 리포트.

## §1. target

- 산출물: docs/v0.7-demo-fixtures/out-t2.md
- 대상 작업: v0.7 시연 Task wf-t2 (3-Task 병렬 집합의 T2 — consumes 계약 X · produces 계약 Y, 실현 A) Execute 산출물

## §2. criteria_basis

- 위임 완료 조건 — docs/v0.7-demo-fixtures/fixture-t2-spec.md §B done 4항 (①②③④). T2 **자기 spec의 done만**이 판정 기준이다(개별 검증 범위 경계 — v0.7-demo-procedure §8-C.3).
- ③의 판정 기준: 계약 X 요지 대응은 fixture-t2-spec.md 내 **인용된 계약 X 문면 구조**(각 용어 헤더 아래 `- 정의:` 줄 정확히 1개) 기준으로 판정한다(위임 input 지정, R2 참조 경계).
- 참고 입력(판정 근거 아님, 06 V1): Worker self_check. 이 주장은 검사 대상으로만 취급하고, 아래 items는 산출물 자체를 근거로 독립 재판정한 결과다.

## §3. items (항목별 판정 4건)

| # | criterion | verdict | evidence | scope | verification_type |
|---|---|---|---|---|---|
| 1 | §B① out-t2.md에 `| 용어 | 정의 요약 |` 2열 표가 존재한다. | 충족(Met) | out-t2.md 7행 헤더 행 `| 용어 | 정의 요약 |`(문면 정확 일치) + 8행 2열 구분 행 존재. 표 전 행(7~11행)이 셀 2개 구조 — 2열 위반 행 0. | out-t2.md 본문 전체(11행) 실측, 표 전 행 열 수 전수 검사. | VT-2 (완료 조건 대조 검증) |
| 2 | §B② 표의 데이터 행 3개가 베타 → 알파 → 감마 순서다. | 충족(Met) | 데이터 행이 9행(베타)·10행(알파)·11행(감마) 정확히 3건, 등장 순서 베타 → 알파 → 감마 일치. 추가·누락 데이터 행 0. | 산출물 표 데이터 행 전수(3/3) — 행 수·순서 동시 대조. | VT-2 (완료 조건 대조 검증) |
| 3 | §B③ 각 행의 정의 요약이 계약 X의 `- 정의:` 줄 요지에 대응한다. | 충족(Met) | 각 행의 정의 요약 셀이 해당 용어의 계약 X `## 용어: <용어>` 헤더 아래 `- 정의:` 줄 요지를 지시(9~11행). 인용된 계약 X 문면 구조(fixture-t2-spec 계약 문면 절 — 헤더 3개, 각각 `- 정의:` 줄 정확히 1개)에 대해 용어↔정의 줄 매핑 3/3 정확 — 교차·누락 대응 0. | **검사 범위(정직 명시):** spec 내 인용된 계약 X 문면 구조 기준으로만 판정 — 형제 Task 산출 실물(out-t1.md)은 불참조(R2 참조 경계·위임 지정). 인용 문면은 구조만 규정하고 구체 정의 본문을 담지 않으므로, 대응 판정은 구조적 매핑(올바른 용어의 `- 정의:` 줄 요지 지시) 수준에서 성립하며 이 범위 안에서 충족이다. | VT-2 (완료 조건 대조 검증) |
| 4 | §B④ 산출 위치가 `docs/v0.7-demo-fixtures/out-t2.md`다. | 충족(Met) | 해당 경로에 파일 실존, 열람 성공(11행, 내용 접근 가능). | docs/v0.7-demo-fixtures/ 내 지정 경로 실측 + 파일 본문 열람. | VT-1 (산출물 존재 검증) |

## §4. final_verdict

**통과(Pass)** — 충족 4 / 위반 0 / 판정 불가 0. 도출: 모든 항목 충족 → 통과 (06 §3.2-C, 결정적 — INV-5).

거짓 완료 보고 검출(06 §3.2-F): Worker self_check(완료 주장)를 참고 입력으로만 수령하고 판정 근거로 사용하지 않았다. 동일 기준 4건을 산출물 실측으로 독립 재판정한 결과, 완료 주장과 재판정 간 모순 0건 — 거짓 완료 보고 미검출.

## §5. verifier_scope

- 검사한 범위: out-t2.md 본문 전체(11행, 표 전 행), 지정 산출 경로 실존 여부, fixture-t2-spec.md §A·계약 문면 절(계약 X 인용)·§B(대조 기준 원본).
- 검사하지 않은·제외한 범위:
  - **확정 계약 Y 문면과의 사후 불일치는 본 판정 범위 밖(Merge 소관).** 본 spec의 Y 실현 지시(계약 X 등장 순 베타→알파→감마)가 확정 계약 Y 문면(가나다 순)과 불일치하는 것은 픽스처의 의도적 보유물(fixture-t2-spec 머리 명시)이며, 개별 검증(CP2)의 판정 대상이 아니다 — Merge 단계 3(충돌 검출)이 검출한다(v0.7-demo-procedure §8-C.3 개별 검증 범위 경계, §8-C.4).
  - 형제 Task 산출 실물(out-t1.md·out-t3.md) — 본 판정 대상 아님, ③은 spec 내 인용 계약 X 문면 구조 기준으로만 판정(R2).
  - 픽스처 디렉터리 밖 저장소 전체(specs/·framework/ 등 — 시연 격리, v0.7 픽스처 경계에 따라 CP2 전수 스캔 제외).
  - 본 리포트의 판정은 이 검사 범위 안에서만 성립한다.
- 비구현(INV-6): 본 검증에서 생성한 저장소 파일은 이 리포트 1개뿐이며, 판정 대상 산출물·픽스처는 수정하지 않았다.

## §6. rework

없음 (final_verdict = Pass).
