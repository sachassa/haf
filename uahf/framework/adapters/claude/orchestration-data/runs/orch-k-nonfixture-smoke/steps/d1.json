{
  "capability": "cap-design",
  "delegation": {
    "constraints": "워크스페이스 밖 파일 생성·수정 금지",
    "context": [
      "Project Orchestration (k) 비픽스처 스모크 — 설계 단위·review 게이트",
      "상위 Contract 는 읽기 전용 참조(재실행 금지)"
    ],
    "done": "python -c \"import io,os;p='design-decision.md';assert os.path.exists(p),p;L=io.open(p,encoding='utf-8').read().splitlines();assert L and L[0].startswith('DESIGN-DECISION: '),repr(L[:1]);assert len(L)>=2,len(L)\" (종료 코드 0 = 통과)",
    "from": "Advisor",
    "input": "빈 워크스페이스(설계 착수)",
    "output": "design-decision.md",
    "task": "현재 작업 디렉터리(이 워크스페이스)에 design-decision.md 파일을 생성하라. 주제: 대시보드 이벤트 타임라인 렌더링의 정렬 키를 seq(이벤트 로그 순서 값)와 물리 timestamp 중에서 선택하고, 그 선택의 근거를 2~3줄로 서술하라. 파일의 **첫 줄은 정확히** `DESIGN-DECISION: <선택값>` 형식이어야 한다(<선택값>은 네 결정 — 예: seq 또는 timestamp). 둘째 줄부터 근거를 적어 **총 2줄 이상**이 되게 하라. 이 워크스페이스 밖의 어떤 파일도 생성·수정하지 마라. Execute 종료 시 done 의 AC 커맨드를 실제로 실행하여(CP1 자체 점검) 종료 코드 0 을 확인한 뒤에만 완료 보고를 내라. 완료 보고 artifacts 에 design-decision.md 경로를 실어라.",
    "to": "Worker"
  },
  "dependsOn": [],
  "done": "python -c \"import io,os;p='design-decision.md';assert os.path.exists(p),p;L=io.open(p,encoding='utf-8').read().splitlines();assert L and L[0].startswith('DESIGN-DECISION: '),repr(L[:1]);assert len(L)>=2,len(L)\" (종료 코드 0 = 통과)",
  "id": "d1",
  "interfaceContract": {
    "produces": [
      "design-decision.md"
    ]
  },
  "model": "haiku",
  "ownedBoundary": [
    "design-decision.md"
  ],
  "role": "Worker",
  "task": "현재 작업 디렉터리(이 워크스페이스)에 design-decision.md 파일을 생성하라. 주제: 대시보드 이벤트 타임라인 렌더링의 정렬 키를 seq(이벤트 로그 순서 값)와 물리 timestamp 중에서 선택하고, 그 선택의 근거를 2~3줄로 서술하라. 파일의 **첫 줄은 정확히** `DESIGN-DECISION: <선택값>` 형식이어야 한다(<선택값>은 네 결정 — 예: seq 또는 timestamp). 둘째 줄부터 근거를 적어 **총 2줄 이상**이 되게 하라. 이 워크스페이스 밖의 어떤 파일도 생성·수정하지 마라. Execute 종료 시 done 의 AC 커맨드를 실제로 실행하여(CP1 자체 점검) 종료 코드 0 을 확인한 뒤에만 완료 보고를 내라. 완료 보고 artifacts 에 design-decision.md 경로를 실어라.",
  "unitType": "design"
}