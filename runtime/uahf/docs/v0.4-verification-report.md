# docs/v0.4-verification-report — v0.4 산출물 독립 검증 리포트 (Task M8, 게이트 C = CP2)

작성일: 2026-07-05
수행 주체: Verifier (Advisor 위임, Task M8 — 독립 판정, 02 §3.2-A)
상태: v0.4 Baseline (CP2 Pass · CP3 승인 · 사용자 승인 2026-07-06. 재검증 r2 — 최초 판정 Fail → demo.md r3 재작업 후 재검증: **final_verdict = Pass** 재도출(§4). 직전 기준선: v0.3 Baseline)
상위 규약: AGENT.md
근거 정본:

- specs/06-verifier.md §3 — 판정 계약(V1~V6, §3.2-A/B/C/D/E/F). 본 리포트의 스키마·판정 값·최종 판정 도출 규칙·재작업 지시 포맷의 정본. (.claude/agents/verifier.md 바인딩 요약 준수.)
- docs/verification-checklist.md — 게이트 C 운용 정본(§1 판정 값, §5.1 VT 적용, §5.3 전수 스캔·상태 서술 대조, §5.4 거짓 완료 검출).
- ROADMAP.md §5 v0.4 — 완료 조건 4건 (R-1~R-4).
- specs/04-memory.md §3·§7 — Memory Core Contract·완료 기준 6건. specs/05-lessons.md §3·§7 — Lessons Core Contract·완료 기준 7건.
- specs/01-runtime.md §3.2-A(Manifest 7필드)·§4.1(디렉터리 바인딩)·§8 예1(contract id `MemoryServiceInterface`) — 정적 대조 기준.
- framework/core/structure.md §2·§5 — Module 구현 디렉터리 경계·금지 토큰 규칙(C-3 확장·적용 범위).
- docs/v0.3-verification-report.md — 리포트 관례 정본(머리 구성·항목 표·세부 근거 절·판정 집계). docs/delegation-protocol.md §2.2 — 완료 보고 필드 대조 기준.
- 위임 메시지 (Advisor → Verifier, Task M8) — criteria·done의 출처.

거버넌스: 이 문서는 `docs/` 소속 검증 산출물이다 (06 §4.1 "검증 리포트 직렬화 — 구조화 파일"). `docs/` 소속이므로 스캔·실측 결과 보고에 필요한 범위 내에서 구체 토큰·물리 경로 인용이 허용된다. 개정은 Advisor 승인 + 본 문서 §9 이력 절 기록으로만 이뤄진다. 문서 머리에 작성일·target·criteria_basis·이력 절을 두는 것은 v0.3-verification-report 관례와 동형이다.

---

## §9. 이력 (Revision History)

| 일자 | 버전 | 변경 | 주체 |
|---|---|---|---|
| 2026-07-05 | v0.4 Draft | 최초 작성. v0.4 (Memory & Lessons, Track A) 산출물 문서 9종 + 물리 실데이터(store 21파일·index 21라인) 독립 검증 — 항목별 판정 31건(criterion/verdict/evidence/scope/verification_type), VT-4 전수 스캔 직접 수행(후보 5부류·범위 기록), VT-5 물리 실데이터 직접 재관측·재계산(store↔index 전수 대조, 스키마 42건 셀 단위, promotion-review 확정값 전건 대조, 회수 재현 재계산, 재발 진리표 재검, 음성 케이스 역산), 거짓 완료 검출 절차 수행. **final_verdict = Fail** (위반 1건 — demo.md 머리 timestamp "실제 수행 시각" 서술 ↔ 물리 ctime/mtime 실측 모순, §3.6). 재작업 지시(06 §3.2-D) 포함(§6). | Verifier (Advisor 위임, Task M8) |
| 2026-07-05 | v0.4 r2 (재검증) | demo.md **r3**(Worker 재작업) 재판정 — §6 revalidation_criteria 전 항목 재수행: timestamp 관련 상태 서술 전 지점 전수 재대조(라이브 실측 모순 주장 잔존 0건 — 잔존 "실제 수행 시각" 리터럴 4곳은 전부 결함·교정 경위 인용으로 판별), §1.1 신설 절의 timestamp 성격(생성기 배정 단조 순서 값 + 일괄 생성 실측 + 04 계약 기능 성립) 명기·정합 확인, memory-data/ 22파일 무변경 실측(크기·mtime 밀리초 단위 원판정 시점과 동일), demo.md §9 이력 r3 행 append-only(r1·r2 행 문면 보존 대조), 타 산출물 8종 무변경 실측, 6시연 (E) 기록·관측 결과 문면 불변 확인. items #31 위반 → **충족** 전이. **final_verdict = Pass** 재도출 (06 §3.2-C). rework = 없음(이행 기록 §6). 상세: §3.7·§4·§5. | Verifier (Advisor 재검증 요청, Task M8 r2) |
| 2026-07-06 | v0.4 Baseline | v0.4 마일스톤 사용자 승인 — 기준선 확정 (CP2 Pass — 재작업 1회 후 재검증, CP3 Advisor 승인). | Advisor |

---

## §0. 이 문서의 위치와 판정 경계

- **이 문서는 검증 리포트(06 §3.2-A) 1건이다.** Verify 연산(06 §3.1)의 출력이며, 판정 대상 산출물·물리 데이터를 수정하지 않았다 (06 INV-6 — 본 검증에서 생성·수정한 저장소 파일은 이 리포트 1개뿐이다).
- **독립성 (V1/INV-1).** 모든 판정은 산출물 자체와 파일 시스템 실측·독립 재계산을 근거로 했다. Worker 완료 보고(M1~M7)·self_check·시연 기록(docs/v0.4-demo.md)의 "충족" 주장은 검사 대상(claim)으로만 취급했고 판정 근거로 삼지 않았다. 시연 기록의 관측 값은 그대로 신뢰하지 않고 물리 store/index 실물 판독과 재계산으로 재판정했다 (§3.2).
- **최종 승인 아님.** 본 리포트는 CP2 독립 판정이다. 최종 승인·재량 판정(CP3)은 Advisor 소관이며, 본 리포트는 조건부·재량 항목을 스스로 통과 처리하지 않는다 (06 §3.2-C).
- **판정의 결정성 (06 INV-5).** 최종 판정은 §3 항목별 판정 집합에서 06 §3.2-C 규칙으로 결정적으로 도출했다(§4). 위반의 경중을 재량으로 감면하지 않았다.

---

## §1. target (판정 대상)

대상 작업: ROADMAP v0.4 (Memory & Lessons, Track A) 전체 산출물 — Task M1~M7 산출 문서 9종 + 물리 실데이터.

| # | 산출물 경로 | VT-1 존재 확인 (실측) |
|---|---|---|
| 1 | `framework/core/structure.md` (v0.4 Draft — M1 개정) | 존재 (23,056 bytes) |
| 2 | `framework/memory/memory-service.md` (r2) | 존재 (28,738 bytes) |
| 3 | `framework/memory/module-manifest.md` (r2) | 존재 (17,887 bytes) |
| 4 | `framework/memory/memory-store.md` | 존재 (16,219 bytes) |
| 5 | `framework/memory/lessons.md` (r2) | 존재 (29,249 bytes) |
| 6 | `framework/adapters/claude/memory-binding.md` (r3) | 존재 (51,001 bytes) |
| 7 | `docs/v0.4-demo-procedure.md` | 존재 (60,619 bytes) |
| 8 | `docs/v0.4-demo.md` (r2) | 존재 (45,792 bytes) |
| 9 | `docs/v0.4-promotion-review.md` (Advisor 직접 산출물) | 존재 (10,498 bytes) |
| 10 | `framework/adapters/claude/memory-data/store/mi-0001.json` ~ `mi-0021.json` | 존재 — **21파일 전수** (616~1,189 bytes, 결손·여분 0) |
| 11 | `framework/adapters/claude/memory-data/index/index.jsonl` | 존재 (5,704 bytes, **21라인** 실측) |

전 대상 실재·접근 가능 — VT-1 충족 (items #1).

---

## §2. criteria_basis (대조 기준 출처)

| 부류 | 기준 | 출처 |
|---|---|---|
| 위임 완료 조건 | ROADMAP v0.4 완료 조건 4건 (R-1~R-4) | ROADMAP.md §5 v0.4 |
| 규격 | specs/04-memory.md §7 완료 기준 6건 (contract id 후단 포함) | 04 §7 (절차서 §2.3이 검증 리포트 소관으로 배정한 정적 2건 포함) |
| 규격 | specs/05-lessons.md §7 완료 기준 7건 | 05 §7 (AI 비의존 전수 스캔 포함) |
| 시연 기준 | v0.4-demo-procedure.md §7~§13 판정 문장 (= 04 §7·05 §7·ROADMAP 인용) — 물리 실데이터 직접 재관측·재계산 | 절차서 §2 1:1 대응 표, 위임 done (4) |
| 규격·경계 | 필수/선택 표기 셀 단위 일치(04·05·01 §3.2 정본) / § 포인터 사슬 실재 / §9 이력 append-only / 부분 개정 잔여(verification-checklist §5.3) | TEMPLATE §3 준용, checklist §5.2·§5.3, 위임 done (5) |
| 경계 규칙 | 소유 경계(v0.3 산출물·specs/ 무변경), 물리 데이터 Adapter 경계 격리, Core/Module 문서 물리 토큰 누출 0 | structure.md §2·§5, 01 §3.2-E, 위임 done (6) |
| 검출 절차 | 거짓 완료 보고 검출 (06 §3.2-F) | 06 §3.2-F, checklist §5.4, 위임 done (7) |

참고 입력(판정 근거 아님, 06 V1): M1~M7 완료 보고의 주장 요지(전건 self_check 충족, M7 "시연 6종 전부 기대 관측대로·위반 0", store↔index 전수 대조 일치)는 위임 메시지로 전달받았으며, 검사 대상으로만 사용했다.

---

## §3. items (항목별 판정 — 31건)

판정 값 정본: 06 §3.2-B (충족 Met / 위반 Violated / 판정 불가 Undetermined). 세부 근거는 §3.2~§3.6에 전개한다.

### §3.1 항목별 판정 표

| # | criterion (대조 기준) | verdict | evidence (근거) | scope (검사 범위) | VT |
|---|---|---|---|---|---|
| 1 | 위임 output/input이 지정한 산출물(문서 9종 + store 21파일 + index 1파일)이 전부 실재·접근 가능하다. | **충족** | §1 표 — 전수 실측 존재(디렉터리 나열 + 문서 전문 정독 + 데이터 전문 판독). | 위임 input 목록 전수. | VT-1 |
| 2 | (R-1) 실패 → Lesson 생성 → 다음 작업에서 회수 사이클을 시연한다 (Learn from Failure). | **충족** | 물리 재검: mi-0001~0008(Candidate, provenance 4필드 실기입) → mi-0009~0016(Active, 승인 참조 = promotion-review §2) 생성·승격 실물 + 회수를 Verifier가 index.jsonl에서 **직접 재계산**(§3.2-E) — Active 8건 중 상황 매칭 {mi-0014(L-06)} 1건만 회수, demo 기록과 일치. 끊김 없는 생성→승격→회수 사슬이 물리 데이터로 성립. | store 21건·index 21라인 전수 + 회수 재계산. | VT-5 |
| 3 | (R-2) 회수 규칙이 최소 Context 원칙을 지킨다 (Token Efficiency). | **충족** | 거부 3케이스(MissingPurpose/MissingScope/UnboundedScope)는 memory-service.md §5.2 단계 1·2가 입력만으로 결정적 — 절차서 §10-B 입력에 대해 Verifier가 프로토콜 논리로 재도출, demo 기록 reason·소속(04 Recall)과 일치. 기본 detail=index: index.jsonl 라인에 content 원문 0건 실측(21/21) — 기본 회수는 물리적으로 원문 로드 불가 구조. `recall.limit.max`=20 선언 실재(module-manifest §3), ⓑ-4 반환 16건 ≤ 20 → truncated=false 재계산 일치. | 거부 3케이스 논리 재도출 + index 전 라인 content 부재 실측 + 상한 대조. | VT-5, VT-3 |
| 4 | (R-3) 모든 Memory 접근이 Memory Service Interface(단일 Port)를 경유한다 (Cross-cutting 계약). | **충족** | 형태 A 규약 실현에서 Port 경유 = memory-binding §3 물리 절차 준수. 물리 결과 정합으로 재판정: store 21 ↔ index 21 **전수 대조 일치**(INV-7 — 우회 기록이면 정합 붕괴 신호), store 구성 21건이 시연 연산 계정과 정확히 일치(여분·고아 0, §3.2-G), 데이터 파일이 Adapter 경계 밖에 0건(§3.5), Core/Module 문서의 접근 서술 전부 Port 경유 선언(memory-service §7, lessons.md §6). PortBypass 흔적 0. | 물리 데이터 전수 + 저장소 전역 데이터 파일 검색 + 문서 접근 서술 전수. 직접 관측 불가 범위는 §5 제한 1. | VT-5, VT-4 |
| 5 | (R-4) Memory 포맷과 Lessons 포맷이 스펙과 일치한다. | **충족** | items #21·#22 — Memory Item 21건·Index Entry 21건·content 내 Lesson/BP/재발 레코드 스키마 셀 단위 전수 대조 위반 0 (§3.2-B·C). | 물리 42건 + content 21건 전수. | VT-3, VT-5 |
| 6 | (04 §7-1) 단일 Port 시연 — 접근 경로가 단일 Port 하나, 백엔드 직접 접근 없음 (INV-1). | **충족** | 항목 4와 동일 근거 + 04 §3.4 소비자 계약 인스턴스(memory-service §7) 정합. | 항목 4와 동일. | VT-5, VT-4 |
| 7 | (04 §7-2) 회수 정책 시연 — purpose/scope 누락 거부(INV-2), 전량 거부(INV-3), 기본 Index Entry만 반환(INV-4). | **충족** | 항목 3과 동일 근거 — 거부 reason 3종 재도출·소속(04 §3.1-B Recall) 정본 대조 일치, 기본 index 반환의 물리 구조(index 라인 content 0) 실측. | 항목 3과 동일. | VT-5 |
| 8 | (04 §7-3) 기록·회수 사이클 시연 — Record 시 Index Entry 생성(INV-7), scope Recall이 인덱스를 거쳐 그 Item만 회수. | **충족** | store 파일명 집합 ↔ index id 집합 **전수 대조 재계산**: 양방향 차집합 0 (21=21, §3.2-A). scope Recall 재현: {kind=lesson, status=Active, situation 겹침} → {mi-0014} 1건 (§3.2-E) — 인덱스만으로 해소 가능함을 Verifier가 직접 수행으로 확인. | 21+21 전수 + 회수 재계산. | VT-5 |
| 9 | (04 §7-3 후단) contract id가 `MemoryServiceInterface`다 — 01 §8 예1 정합 (절차서 §2.3이 검증 리포트 소관으로 배정한 정적 대조). | **충족** | 3중 정적 대조 일치: framework/memory/module-manifest.md §2 `contract` = `MemoryServiceInterface` ↔ specs/01-runtime.md §8 예1("contract = MemoryServiceInterface를 구현하는 Module") ↔ specs/04-memory.md §4.1("contract = `MemoryServiceInterface` (01 §8 예1과 정합)"). memory-service.md §5.2 예·§7, memory-binding §2 #1도 동일 값 인용 — 불일치 0. | 3정본 + 인용 지점 전수. | VT-3 |
| 10 | (04 §7-4) 포맷 일치 시연 — Memory Item·Index Entry가 §3.2-A/§3.2-C 스키마와 일치. | **충족** | 임의 표본이 아니라 **전건**: store 21건 = 6필드(`id`/`kind`/`content`/`source`/`timestamp`/`labels`) 정확히, 여분 필드 0 / index 21라인 = 6필드(`id`/`kind`/`source`/`timestamp`/`labels`/`digest`) 정확히, `content` 필드 0건 (§3.2-B). | 물리 42건 셀 단위 전수. | VT-5, VT-3 |
| 11 | (04 §7-5) 경계 시연 — kind·content 불투명(INV-5), Lesson·BP 상세 스키마를 Memory가 정의하지 않음. | **충족** | memory-service §4.1·memory-store §0·§2가 kind/content 상세를 05·lessons.md 소관 포인터로만 처리(전문 표 중복 0 — r2 이중 갱신 방지 확인). 물리 데이터에서 Adapter가 content 내부를 해석·변형한 흔적 0 — content는 promotion-review 원문 그대로 불투명 직렬화(§3.2-D), index에는 content 미노출. | framework/memory 4문서 경계 서술 전수 + 물리 21건. | VT-3, VT-5 |
| 12 | (04 §7-6) AI 비의존 — framework/memory/ 4문서 + structure.md 본문 전수 토큰 스캔 0건 (검증 리포트 소관 배정분). | **충족** | VT-4 전수 스캔(§3.3) — 후보 5부류 전체 실증 0건. 유일 매치 부류는 프로젝트 문서 파일명 참조(`session-handoff-v0.x`)뿐 — 금지 토큰 아님(v0.3 리포트 §3.2 분류 선례 동형). Worker self_check 스캔을 근거로 삼지 않고 직접 수행 (06 V1). | 5파일 전문, 후보 집합 §3.3 명시(단일 토큰 검사 아님). | VT-4 |
| 13 | (05 §7-1) 학습 사이클 시연 — 실패 보고 → Register Candidate → Verifier Learn 입력 → Advisor 승격 → Active → 다음 작업 Consult 회수. | **충족** | 항목 2와 동일 물리 사슬 + 승격 3조건의 근거 실재: 5필드 채움(물리 content 실측), 실패 실재 근거(handoff §1.4류 — 출처 § 실재 확인), Advisor 승인(promotion-review §2 전건 [승인] — 문서 실재·전건 확인). | 항목 2와 동일 + promotion-review 전문. | VT-5 |
| 14 | (05 §7-2) 최소 회수 — 전량이 아니라 매칭 최소 집합만 반환 (INV-6). | **충족** | Verifier 직접 재계산(§3.2-E): Active lesson 8건 중 situation 겹침>0은 mi-0014 유일(겹침 2, 나머지 7건 겹침 0) → 최소 집합 {mi-0014} = 1/8, 전량 아님. demo 기록과 동일 도출. | index 라벨 전수 재계산. | VT-5 |
| 15 | (05 §7-3) 단일 Port — 모든 저장·회수가 Port 경유 (INV-1). | **충족** | 항목 4·6과 동일 근거 + lessons.md §6(단일 Port·백엔드 직접 접근 금지 선언)·memory-binding §5.3(세 kind 동일 백엔드·분기 없음) 정합. | 항목 4와 동일. | VT-5, VT-4 |
| 16 | (05 §7-4) 승격 권한 — Advisor 승인 없는 승격이 NotApproved로 차단 (INV-4). | **충족** | 음성 케이스 역산(§3.2-G): store 21건 구성이 승인 경로 연산만으로 완전 계정(8C+8A lesson, 1C+1A BP, 3 재발) — 음성 시도(L-08 무승인 Promote)의 물리 산출물 0건 확인. 양성 대조 mi-0016(L-08 Active) 실재, 승인 참조 사슬 = promotion-review §2 L-08 [승인] 실재. reason `NotApproved` 소속 = 05 §3.1-A Promote — 04 reason과 혼입 0 (demo §5.2·절차서 §6-4 대조 일치). | store 전수 역산 + 정본 reason 소속 대조. | VT-5, VT-3 |
| 17 | (05 §7-5) 재발 판정 — Recurrence/RecallGap/Novel 3분류 (INV-7). | **충족** | 진리표 재검(§3.2-F): mi-0019 (매칭 없음 → Novel, matched=null·was_recalled=null) / mi-0020 (L-07 Active 실재[mi-0015 실측] + 회수집합{L-06} 미포함 → was_recalled=false → RecallGap) / mi-0021 (L-06 Active 실재[mi-0014] + 회수집합 포함 → was_recalled=true → Recurrence) — 05 §3.1-C·lessons.md §4 진리표와 3/3 일치, "정확히 하나" 분류 성립. 회수 이력 수동 제공(03 소관 입력)이 레코드·기록에 명시됨. | 물리 3건 필드 전수 + 매칭 대상 Active 실재 확인. | VT-5 |
| 18 | (05 §7-6) Best Practice 대칭 — 성공 완료 보고 → BP 생성 → 회수가 Lesson과 동일 계약 (INV-8). | **충족** | mi-0017(Candidate)→mi-0018(Active) 물리 실재, content = 05 §3.2-B 7필드 대칭 구조(success/practice ↔ failure/cause+lesson, id·status·applicability·provenance 공유 — 셀 단위 확인). 회수 재계산: {kind=best-practice, status=Active, situation∋rework-execution} → {mi-0018} 1건 — Lesson 회수와 동일 index-first 경로·동일 물리 규칙(item당 파일·append·index 정합). | 물리 2건 전수 + 회수 재계산 + 스키마 대칭 대조. | VT-5, VT-3 |
| 19 | (05 §7-7) AI 비의존 — 특정 AI 모델명·제품 기능·매칭 구현 참조 0건 (lessons 대상 포함 전수 스캔). | **충족** | 항목 12와 동일 스캔(§3.3). lessons.md의 "(키워드·의미 검색·임베딩 등)"은 Frozen 정본 05 §3.2-C 원문 인용(비규정 대상의 범주 언급)으로 분류 — 특정 매칭 엔진·제품명 아님. | 항목 12와 동일. | VT-4 |
| 20 | (done 4-a) store 파일명 집합 ↔ index.jsonl id 집합 전수 대조 재계산 (INV-7). | **충족** | Verifier 직접 재계산: store `*.json` = {mi-0001..mi-0021} 21개, index 라인 id = {mi-0001..mi-0021} 21개 — store-only 0, index-only 0, 중복 0. index 순서 = id 순서 = 물리 생성 순서(ctime 실측 단조) 정합. | 21+21 전수 (표본 아님). | VT-5 |
| 21 | (done 4-b) Memory Item 21건·Index Entry 21건 스키마 셀 단위 대조 (04 §3.2-A/C = memory-store §2·§3), Index Entry content 원문 0건 (INV-4). | **충족** | 42건 전수 판독(§3.2-B): 필드 집합·타입·의미 위반 0. `labels` 사용 키는 memory-binding §5.4 정본 5키(`situation`/`stable_id`/`status`/`verdict`/`matched_lesson_id`) 내에서만 사용 — 정본 밖 키 0. digest는 짧은 서술로 content 원문 아님(21/21). | 물리 42건 전수. | VT-5, VT-3 |
| 22 | (done 4-c) Lesson·BP·재발 판정 레코드의 content ↔ 05 §3.2-A/B/E 스키마 대조. | **충족** | Lesson 16건: id/status/failure/cause/lesson/applicability/provenance(4필드) 전부 채움, 선택 `supersedes` 미기입(해당 없음 — 정본 표기 보존). BP 2건: id/status/success/practice/applicability/provenance. 재발 3건: 5필드(failure_signature/matched_lesson_id/was_recalled/verdict/follow_up) 전부 존재 — mi-0019의 null 2건은 "(있으면)" 선택 의미와 정합. 위반 0 (§3.2-C). | content 21건 전수. | VT-5, VT-3 |
| 23 | (done 4-d) 등록 content가 promotion-review §2·§3 확정값과 일치 — 표본 아닌 전건, 개작 0. | **충족** | 전건 원문 대조(§3.2-D): L-01~L-08 × (Candidate+Active) 16건의 failure/cause/lesson/applicability/provenance 4필드, BP-01 × 2건의 success/practice/applicability/provenance — promotion-review 확정값과 문자 단위 일치, 개작·추측 0건. Candidate↔Active 쌍은 status만 상이. | 18건 × 전 필드 전수. | VT-5 |
| 24 | (done 4-e) 시연 ⓐ 회수의 독립 재현 — index.jsonl에서 kind=lesson·status=Active·situation 겹침으로 후보 재계산 → demo 기록(최소 집합 {L-06})과 일치. | **충족** | 재계산(§3.2-E): status=Active 필터 → 8건(mi-0009~0016); situation ∩ {document-revision, status-decision-description-update}: mi-0014=2, 나머지 7건=0 → 결과 {mi-0014(L-06)} — demo §3 기록과 동일. 부가 재현: ⓑ-4 {kind=lesson} → 16건(=demo 기록), ⓔ {kind=best-practice·Active·rework-execution} → {mi-0018}(=demo 기록). | index 전 라인 재계산 3케이스. | VT-5 |
| 25 | (done 4-f) 시연 ⓓ 3분류 판정 논리를 절차서 §12-B 진리표 기준 재검. | **충족** | §12-B 진리표(매칭 유/무 × 회수집합 포함 여부)에 물리 레코드 3건을 대입 — 3/3 정확 분류(항목 17 상세). follow_up 서술도 05 §3.1-C 후속 조치 정의와 정합. | 3케이스 전수. | VT-5 |
| 26 | (done 4-g) 시연 ⓒ 음성 케이스가 물리 파일을 만들지 않았다 (store 21건 구성 역산). | **충족** | 역산(§3.2-G): 21 = lesson Candidate 8 + lesson Active 8 + BP Candidate 1 + BP Active 1 + 재발 3 — 시연 연산 계정과 잔여 0으로 일치. 무승인 Promote 시도의 산출 파일이 존재하려면 22건 이상이어야 함 → 0건 확인. | store 전수 + kind/status 분포 재계산. | VT-5 |
| 27 | (done 5) 필수/선택 표기 셀 단위 일치(04·05·01 §3.2 정본) + § 포인터 사슬 실재. | **충족** | 셀 단위 대조(§3.4): memory-service §4.2↔04 §3.2-B(purpose 예/scope 예/detail 아니오(기본 index)/limit 아니오)·§4.4↔04 §3.2-D / memory-store §2↔04 §3.2-A(id "예(Record가 할당)"·labels 아니오)·§3.2↔04 §3.2-C(digest "(선택)") / module-manifest §2↔01 §3.2-A(필수 4·선택 3, "(기본 없음)"·"(기본 true)" 보존) / lessons §2.1~2.6↔05 §3.2-A~F(supersedes 아니오 포함) — 표기 불일치 0. 포인터 사슬: memory-service §4.1→memory-store §2·§4.3→memory-store §3 실재 / lessons §5.2 r2 투영 3건(situation·stable_id·status) ↔ memory-binding §5.4 5키 정합(잔여 2키는 재발 레코드 필드 — 필드 정본 lessons §2.5, §3.4 관찰 1) / 절차서 §2 인용 문장 ↔ ROADMAP·04 §7·05 §7 원문 일치. | 대상 문서 전 필드 표 + 인용 § 전수. | VT-3 |
| 28 | (done 5) 각 문서 §9 이력 append-only + 부분 개정 잔여(같은 상태 서술 전 지점) 0건. | **충족** | 이력: 9문서 전부 머리 이력 절 보유, 행 연대순 append(r2·r3 행이 기존 행 보존 — memory-binding r3의 r1 행 원복은 이력 행 시점 기록 원칙의 복원으로 append-only 정합). 잔여: demo.md r2 재부여(§10·§11) 후 구 번호 참조 잔존 0건(`§14|§15` 전문 검색 0) / memory-service r2의 §4.1·§4.3 포인터화 참조 지점(§1·§2 표·§4 도입·§8·§10) 전수 정합 / module-manifest r2 확정형 전수(§2·§3·§5·§6 "확정 대기 잔여 0" 실제 정합) / memory-binding §7 실측 서술 ↔ 현 파일 시스템 일치(21파일·21라인·5,704 bytes·kind 분포 16/2/3·라벨 5키 — 전항 재실측 일치). | 9문서 이력 절 + 개정 참조 지점 전수. | VT-3 |
| 29 | (done 6) 경계 검증 — v0.4가 지정 경계 밖 파일을 수정하지 않음(v0.3 산출물·specs/ 무변경), 물리 데이터는 Adapter 경계 이하에만, Core/Module 문서에 물리 경로·형식 토큰 누출 0. | **충족** | 실측(§3.5): mtime 타임라인 분리 — specs/ 전 15파일 08:07, v0.3 산출물 7종 20:15:39~20:16:11(v0.3 Baseline 스탬프 — 각 문서 상태 줄·이력 행 실측 확인, v0.4 이전), v0.4 산출물 21:15:05~22:48:46. v0.3 산출물·specs/에 v0.4 구간 mtime 0건. 저장소 데이터 파일(`*.json`/`*.jsonl`) 검색 결과 전량 `framework/adapters/claude/memory-data/` 이하(22개) — 경계 밖 0. framework/ 인벤토리: core 2·runtime 3·memory 4·adapters/claude 2문서+memory-data, loop/verifier/workflow/plugins 빈 상태(무단 산출 0). 토큰 누출: §3.3 스캔에서 Core/Module 5문서에 `memory-data`·직렬화 형식명·확장자·구체 어댑터명 0건. | 파일 시스템 전수 + 스캔 §3.3. | VT-4, VT-1 |
| 30 | (done 7) 완료 보고 실질 주장 재판정 — self_check 충족 주장·"시연 6종 기대 관측"·store↔index 일치 주장이 실측과 모순되지 않는다. | **충족** | 참고 입력의 실질 주장 전부를 독립 재판정: 시연 6종의 판정 문장별 관측(items #2~#8, #13~#18, #24~#26)·전수 대조(#20)·스키마(#21·#22)·개작 0(#23) — 재계산·실측이 주장과 전 지점 일치, 모순 0. 단 시연 기록의 timestamp 서술 1건은 별도 항목 #31에서 위반으로 판정 — 실질 관측 값(건수·집합·분류·대조)의 거짓은 아니며 서술 계층 결함이다. | 참고 입력 주장 전수 ↔ §3.2 실측. | VT-5, VT-4 |
| 31 | (06 §3.2-F·checklist §5.3) 시연 기록(demo.md)의 상태·관측 서술이 물리 실측과 전수 일치한다 — 실측 불일치 0건. | **충족** (r3 재판정 — 최초 판정 위반, §3.6·§3.7) | 최초 판정(demo.md r2 시점)에서 위반 1건 검출 — 머리(수행 일시 행)가 물리 기록 21건 timestamp(22:19:47.602~22:20:07.602)를 "(Record 순서 단조 증가, **실제 수행 시각**)"으로 서술했으나, 실측상 store 21파일 ctime/mtime = **22:19:46.604~46.628 일괄 생성**으로 전 21건의 timestamp가 물리 생성보다 +0.998s~+20.974s 미래의 생성기 배정 값 — 실제 기록 시각일 수 없음(L-07 유형, §3.6 원문 보존) → **r3에서 해소 확인** (§3.7): 라이브 서술 전 지점(머리·§1.1 신설·§8.3·§10)이 "생성기 배정 단조 증가 순서 값 + 물리 일괄 생성 실측(22:19:46±25ms)"으로 정합 교정되었고, 라이브 실측 모순 주장 잔존 0건, 물리 데이터 무수정 실측. | demo.md r3 전문의 timestamp·시각 관련 서술 전 지점 재열거·재대조 + 22파일 크기·mtime 밀리초 재실측 (§3.7). | VT-4, VT-5 |

### §3.2 세부 근거 A — VT-5 물리 실데이터 직접 재관측·재계산 (done 4, V1)

시연 기록(demo.md)의 관측 주장을 신뢰하지 않고, `framework/adapters/claude/memory-data/` 실물(store 21파일 전문 + index.jsonl 21라인 전문)을 직접 판독·재계산했다.

- **A. store↔index 전수 대조 (done 4-a).** store 파일명 집합(확장자 제외) {mi-0001..mi-0021} ↔ index.jsonl 각 라인 `id` 집합 — 양방향 차집합 0, 중복 0. index append 순서 = id 사전순 = 물리 생성 순서(ctime 밀리초 단조 증가 실측) — 3중 정합.
- **B. 스키마 셀 단위 (done 4-b).** store 21건 최상위 키 = 정확히 {`id`,`kind`,`content`,`source`,`timestamp`,`labels`} (여분·결손 0). index 21라인 키 = 정확히 {`id`,`kind`,`source`,`timestamp`,`labels`,`digest`} — **`content` 키 0건** (04 INV-4). store↔index 동일 id의 kind/source/timestamp/labels 값 일치.
- **C. content 스키마 (done 4-c).** kind 분포 = lesson 16(Candidate 8·Active 8) / best-practice 2(C1·A1) / recurrence-judgment 3(Novel·RecallGap·Recurrence 각 1) — 합 21. 각 content가 05 §3.2-A(8필드 중 필수 7 채움·선택 supersedes 미기입)/§3.2-B(7필드 중 필수 6)/§3.2-E(5필드) 정본 구조와 일치. provenance는 전건 4필드(task_ref/report_ref/origin_role/ordering_ref) 실기입.
- **D. promotion-review 확정값 전건 대조 (done 4-d).** L-01~L-08·BP-01의 확정 필드값(§2·§3)과 물리 content를 전건·전 필드 원문 대조 — **개작·추측 0건**. 출처 사슬 실재: session-handoff-v0.1.md §5.3 항목 4(1건)·v0.2.md §1.5(3건)·v0.3.md §1.5(4건) 실측 — 재열거 합 8건, 라벨 표·promotion-review와 1:1.
- **E. 회수 재현 (done 4-e).** Verifier가 index.jsonl만으로 시연 ⓐ의 Recall을 재수행: (1) kind=lesson 필터 → 16; (2) labels.status=Active → 8 (mi-0009~0016); (3) labels.situation ∩ {document-revision, status-decision-description-update} 겹침 계산 → mi-0014만 겹침 2, 나머지 0 → **결과 {mi-0014(L-06 Active)}, 1/8 최소 집합** — demo §3 기록과 동일. store 파일을 열지 않고 해소됨(index-first 물리 성립). ⓑ-4(kind=lesson → 16건, 16≤20 → truncated=false)·ⓔ(best-practice·Active·rework-execution → {mi-0018})도 재계산 일치.
- **F. 재발 진리표 재검 (done 4-f).** items #17·#25 — 3/3 정확 분류, 매칭 대상 Active(L-07=mi-0015, L-06=mi-0014)의 물리 실재 확인 포함.
- **G. 음성 케이스 역산 (done 4-g).** 21건 구성이 승인 경로 연산만으로 완전 계정(잔여 0) → 무승인 Promote의 물리 산출 0건. L-08 Active(mi-0016)의 승인 참조 사슬(promotion-review §2 L-08 [승인]) 실재.

### §3.3 세부 근거 B — VT-4 전수 스캔 (done 2-b, 3)

**대상 범위**: `framework/memory/memory-service.md`·`module-manifest.md`·`memory-store.md`·`lessons.md` + `framework/core/structure.md` — 5파일 **전문** (표·예시·거버넌스 문단 포함). Adapter 경계 문서(memory-binding.md)는 격리 보유 지점으로 제외 (structure.md §5 C-3 — 위임 done 명시). Verifier가 직접 수행 — Worker self_check 스캔을 근거로 삼지 않았다 (06 V1). 단일 토큰 검사가 아니다.

**금지 토큰 후보 집합** (대소문자 무시, 5부류 전체):

1. 특정 AI 이름·모델명·제품 기능명: `claude, anthropic, opus, sonnet, haiku, fable, gpt, openai, chatgpt, gemini, bard, llama, mistral, cohere, deepseek, grok, qwen, copilot, subagent, 서브에이전트, mcp, llm, assistant, api, sdk, cli, ide, prompt, 프롬프트, session, 세션, settings, .claude`
2. 프로그래밍 언어명: `python, javascript, typescript, java, rust, kotlin, swift, ruby, php, perl, scala, haskell, bash, powershell, shell, sql, html, css, c++`
3. 툴체인명: `npm, pip, cargo, yarn, node, docker, kubernetes, github, maven, gradle, webpack, vite, git`
4. 직렬화 형식명·확장자·OS 토큰: `json, jsonl, yaml, yml, xml, toml, markdown, front-matter, csv, protobuf, .ps1, .py, .js, .ts, .exe, windows, linux, macos, unix`
5. 물리 경로·인스턴스 누출: `memory-data, adapters/claude, mi-00, index.jsonl` (C-3 확장 — Core/Module 문서의 물리 인스턴스 비서술 검증)

**결과: 금지 토큰 실증 0건.** 매치 전량 분류:

| 매치 | 위치 | 분류 (금지 토큰 아님의 근거) |
|---|---|---|
| `session-handoff-v0.2` / `session-handoff-v0.3` | 5파일 거버넌스 문단·Lesson 후보 참조 (memory-service L21·L52·L54·L120·L199·L249, module-manifest L18·L50·L72·L137, memory-store L18·L143, lessons L14, structure L13) | 프로젝트 문서 파일명 참조(`docs/session-handoff-v0.x.md`, 실재) — 저장소 내부 상호 참조. v0.3 검증 리포트 §3.2 분류 선례와 동형. |
| (부류 2~5) | — | **매치 자체 0건** — 언어·툴체인·직렬화 형식·확장자·OS·물리 경로 토큰이 5문서 본문에 전무. |

참고: lessons.md §5.2의 "(키워드·의미 검색·임베딩 등)"은 Frozen 정본 05 §3.2-C 원문의 인용(규정하지 **않는** 대상의 범주 열거)이며 특정 매칭 엔진·제품명이 아니다 — v0.3 리포트의 "세션 경계" 범주 언급 분류와 동형.

### §3.4 세부 근거 C — 문서 정합 (done 2-a, 5)

- **contract id 3중 대조 (done 2-a, items #9).** module-manifest.md §2 `contract` 값, 01 §8 예1, 04 §4.1 — 세 지점 모두 `MemoryServiceInterface` 동일 문자열. 인용 지점(memory-service §5.2 예·§7·§10, memory-binding §2 #1·§4.1·§6 SP-5, module-manifest §1·§3·§6) 전수 동일.
- **필수/선택 표기 셀 단위 (items #27).** 4개 인스턴스 문서의 전 필드 표를 04 §3.2-A/B/C/D·05 §3.2-A~F·01 §3.2-A 원문과 대조 — 필드명·의미·필수/선택 표기(괄호 주석 포함) 불일치 0. Recall Result·Failure Report의 "필수 열 없음"도 정본 그대로 보존됨(2열 표).
- **포인터 사슬 (items #27).** memory-service §4.1→memory-store §2 / §4.3→memory-store §3: 대상 절 실재·내용 정합(이중 갱신 방지 구조 성립 — memory-service에 전문 표 중복 0 확인). lessons §5.2 r2 투영(적용 조건→`labels`, 안정 id→`labels`, status→`labels`, 종류→`kind`, 최신 상태 해소) ↔ memory-binding §5.4 라벨 키(`situation`·`stable_id`·`status`) ↔ 물리 index 실사용 키 — 3층 일치. §5.4의 나머지 2키(`verdict`·`matched_lesson_id`)는 재발 판정 레코드 필드(정본 lessons §2.5/05 §3.2-E)의 index 조회용 표기로 memory-binding §5.3이 근거를 명시 — 물리 데이터와 일치.
- **관찰 1 (위반 아님 — 기록만).** `verdict`·`matched_lesson_id`의 "labels 투영" 결정 자체는 lessons.md §5.2(05 소유 투영 규칙)에 명시 항목이 없고 memory-binding §5.3~§5.4에서 서술된다. `labels`는 04 §3.2-A 자유 태그 집합이고 필드 정본 포인터가 05를 가리키므로 계약 재정의는 아니나, "무엇을 투영하는가는 05 소유" 원칙의 완결성을 위해 차기 lessons.md 개정 시 재발 레코드 투영 2건의 명시 편입이 정리 후보다. 비차단.
- **관찰 2 (위반 아님 — 기록만).** structure.md §8 트리는 v0.3 산출물 예시 + 경계 주석 구성으로, v0.4 신규 파일(framework/memory/ 4문서·memory-binding.md·memory-data/)을 열거하지 않는다. 트리가 전수 파일 목록을 주장하지 않고 Module 구현 디렉터리 행에 "v0.4 첫 실사용: memory/" 주석이 실재하므로 상태 서술 불일치는 아니다. 차기 개정 시 갱신 후보.
- **절차서·시연 기록 정합.** 절차서 §2 대응표의 인용 문장은 ROADMAP §5 v0.4·04 §7·05 §7 원문과 일치(재정의 0). demo.md의 판정 문장은 절차서 (D) 전재 — 대조 일치. 절차서가 검증 리포트 소관으로 배정한 정적 3건(contract id·04/05 AI 비의존)은 본 리포트 items #9·#12·#19가 수행했다.

### §3.5 세부 근거 D — 경계 검증 (done 6)

- **mtime 타임라인 실측 (소유 경계).** specs/ 15파일 = 2026-07-05 08:07 / delegation-protocol·verification-checklist = 09:19 / v0.3 산출물 7종(config-schema·module-manifest(runtime)·module-registry·lifecycle·runtime-binding·v0.3-demo-procedure·v0.3-demo) = 20:15:39~20:16:11 / v0.4 산출물 = 21:15:05(structure M1)~22:48:46(memory-binding r3). v0.3 산출물·specs/에 v0.4 작업 구간(21:15 이후) mtime **0건**.
- **v0.3 산출물 크기 차이의 해명.** 현재 크기가 v0.3 검증 리포트 §1 기록 대비 +156~+220 bytes — 전량 v0.3 Baseline 승인 스탬프(각 문서 상태 줄 "v0.3 Baseline" + §9 이력 Baseline 행, Advisor, 20:15~20:16 일괄)로 실측 확인(7종 전건에서 해당 상태 줄·이력 행 존재 확인). v0.4 작업에 의한 변경이 아니다.
- **물리 데이터 격리.** 저장소(framework/·docs/·specs/) 전역 `*.json`/`*.jsonl` 검색 → 22개 전량이 `framework/adapters/claude/memory-data/` 이하. Core·specs·docs 경계에 데이터 파일 0건 — memory-binding §0 위치 정본 선언과 일치.
- **디렉터리 인벤토리.** framework/core 2파일·runtime 3파일·memory 4파일·adapters/claude {runtime-binding.md, memory-binding.md, memory-data/}·loop/verifier/workflow/plugins 4디렉터리 빈 상태 — 완료 보고 밖 무단 산출물 0건 (L-05 유형 재발 없음). demo.md §8.4 artifacts 목록(디렉터리 3·파일 23)과 실측 일치.

### §3.6 세부 근거 E — 검출 위반 상세 (items #31) + 영향 분석

**위반 — demo.md timestamp "실제 수행 시각" 서술 ↔ 물리 실측 모순 1건.**

- 서술 (demo.md 머리, 수행 일시 행): "물리 기록 21건 timestamp 범위 `2026-07-05T22:19:47.602+09:00` ~ `2026-07-05T22:20:07.602+09:00` (Record 순서 단조 증가, **실제 수행 시각**)".
- 실측: store 21파일의 CreationTime·LastWriteTime = 22:19:46.604 ~ 22:19:46.628 — **25ms 창 내 일괄 생성**. index.jsonl·store/·index/ 디렉터리 mtime 동일 22:19:46. 반면 기록 내 `timestamp` 값은 22:19:47.602부터 정확히 1.000초 간격(밀리초 .602 공통)으로 22:20:07.602까지 — **전 21건이 자기 파일의 물리 생성 시각보다 +0.998s~+20.974s 미래**다. 물리적으로 각 기록의 실제 기록 시각일 수 없다(파일이 자신의 "기록 시점"보다 먼저 존재할 수 없음). demo.md §1.4가 공개한 "결정적 생성기" 방식과 정합하는, **생성기 배정 스케줄 값**이다.
- 판정 근거: verification-checklist §5.3("서술 ↔ 실제 불일치 1건이라도 있으면 위반")·06 §3.2-F(주장-산출물 모순 검출). L-07("실재/존재 등 상태 주장은 실측 후에만 기입")과 동일 결함 부류 — 시각 주장을 실측 없이 기입했다. 본 시연이 L-07을 Active로 승격한 세션에서 발생한 자기 유형 재발이라는 점도 기록한다.
- **영향 분석 (계약 기능 훼손 없음 — 위반 범위의 한정).** (1) 04 §3.2-A `timestamp`의 계약 기능(scope `timeRange` 해소·04 INV-6 위 최신 상태 해소·05 ordering_ref 순서 판정)은 **훼손되지 않았다** — 기록 순서·물리 생성 순서·timestamp 순서가 3중 일치하고, 값 편차는 동일 세션 창 내 ≤21초이며, 구체 시간 포맷·시각 원천은 직렬화(Adapter) 소관이다(05 §3.2-D). (2) 시연 6종의 실질 관측(건수·집합·분류·전수 대조·개작 0)은 전부 독립 재검증에서 일치(items #30) — 수행하지 않은 단계의 관측을 창작한 것이 아니다. (3) 따라서 위반은 물리 데이터 자체가 아니라 **demo.md의 서술 계층 1건**에 국한된다. 재작업 최소 경로는 서술 교정이다(§6).

(위는 최초 판정의 검출 기록이다. 검출 근거의 이력 보존을 위해 원문을 유지하며, 해소 확인은 §3.7이 담당한다.)

### §3.7 세부 근거 F — demo.md r3 재검증 (Worker 재작업 후, §6 revalidation_criteria 재판정)

Worker가 demo.md **r3**(§9 이력 r3 행: "CP2 재작업 지시 반영 — 검증 리포트 item #31")로 재작업을 완료했고, Verifier가 §6의 revalidation_criteria 전 항목을 재수행했다. r3 완료 보고를 신뢰하지 않고 r3 전문 재정독 + 물리 재실측으로 판정했다 (V1). 표본 대조가 아니다.

- **A. timestamp 상태 서술 전 지점 전수 재대조 — 라이브 실측 모순 주장 잔존 0건 (revalidation 1 충족).** r3 전문에서 timestamp·시각 관련 서술 전 지점을 전수 열거·대조했다: ① 머리 수행 일시 행 — "물리 파일 생성(store 21 + index 1)은 세션 중 일괄 수행 — 실측 파일 mtime 22:19:46±25ms(일괄). 기록 내 timestamp 값(…1초 간격)은 파일별 실제 쓰기 시각이 **아니라** … 생성기가 배정한 **단조 증가 순서 값**" — 실측 정합(실측 창 46.604~46.629, 폭 25ms, 초 단위 앵커 22:19:46 — 주장 정밀도 수준에서 정확) ② §1.1 신설 bullet — 순서 값 성격 + 일괄 생성 실측 + 04 §3.2-A 계약 기능(Record 순서 정합·lessons.md §5.2 최신 상태 해소) 성립 명기 — 본 리포트 §3.6 영향 분석과 정합 ③ §8.3 신규 bullet — CP2 검출 결함·교정 경위·**L-07 유형 재발** 명시·물리 파일 무수정·**v0.4 신규 Lesson 후보 근거** 기록(은폐 없음 — Learn 입력 정합) ④ §10 self-note — r2의 "timestamp는 실제 수행 시각" 주장이 교정 서술로 대체 ⑤ §9 r3 이력 행 — 교정 대상 라이브 주장 2지점(head·§10) 전수 열거 명시(L-06 열거 목록 보고 충족). 잔존 "실제 수행 시각" 리터럴 4곳(§9 r3 행·§1.1 말미·§8.3·§10)은 **전부 초판·r2 결함의 인용·교정 경위 서술**이며 현재형 상태 주장이 아니다 — 라이브 실측 모순 서술 0건.
- **B. 물리 데이터 무변경 실측 (revalidation 2 충족).** memory-data/ 22파일 재실측: store 21파일 크기(mi-0001 1,159B … mi-0021 686B)·mtime(22:19:46.604~46.628) + index.jsonl(5,704B·22:19:46.629) — 최초 판정 시점 실측과 **밀리초 단위 동일**, 전부 r3 개정 시각(23:12:19) 이전. 기록 불변(04 INV-6) 유지 — r3는 물리 데이터를 건드리지 않았다.
- **C. 이력 append-only (revalidation 3 충족).** demo.md §9 r1·r2 행 문면이 r2 시점 원문과 동일하게 보존되고 r3 행이 append되었다. r3 행의 서술(검출 경위·교정 범위·물리 무수정)이 실제 변경 내용과 일치.
- **D. 타 산출물 무변경 (revalidation 4 충족).** 나머지 8종(structure.md·framework/memory 4문서·memory-binding.md·demo-procedure·promotion-review) 크기·mtime 재실측 — 최초 판정 시점과 전부 동일. 갱신된 파일은 demo.md(50,101B, 23:12:19) 1개뿐.
- **E. 교정 서술의 계약 정합 (revalidation 5 충족).** §1.1의 "04 §3.2-A timestamp 계약 기능이 순서 값으로 성립" 서술은 04 원문("기록 시점" — 포맷·원천 무규정)·05 §3.2-D(ordering_ref "구체 시간 포맷은 직렬화 소관")·lessons.md §5.2(최신 상태 해소 — 본 데이터에서 Active가 Candidate보다 뒤 순서 값으로 실측 성립)와 모순 없음.
- **F. 그 외 기록·판정 문장 무변경 확인.** 6시연 (E) 기록 표(판정 문장·관측 결과 "충족"·근거·수행 물리 지점)·§8.1·§8.2·§8.4·§11이 r2 문면과 동일함을 전문 대조로 확인. §8.3 첫 bullet 표제의 명료화("불일치 없음"→"시연 관측 불일치 없음")는 CP2 검출 결함 병기와 정합하는 서술 정리로 관측 주장 변경이 아니다. items #1~#30의 기존 충족 판정에 영향을 주는 변경 0건 — items #28(이력 append-only·부분 개정 잔여)은 r3 반영 후에도 성립(A·C 재대조).

---

## §4. final_verdict (06 §3.2-C 결정적 도출)

**최초 판정 (demo.md r2 시점):** 충족 30 / 위반 1 (items #31) / 판정 불가 0 → 하나라도 위반 → **Fail**. (위반 상세 §3.6. Verifier는 위반의 경중을 재량으로 감면하지 않았다 — 06 §3.2-C의 도출은 결정적이다.)

**재검증 판정 (demo.md r3, Worker 재작업 후 — §3.7):** items #31이 revalidation_criteria 전 항목 재수행으로 충족 전이. 집계: **충족(Met) 31건 / 위반(Violated) 0건 / 판정 불가(Undetermined) 0건.**

- 도출 규칙 적용: 모든 항목이 충족 → **통과(Pass)**.

**final_verdict = Pass.**

주: 동일한 항목별 판정 집합은 항상 동일한 최종 판정을 낸다 (06 INV-5) — 최초 Fail과 재검증 Pass는 판정 집합 자체가 변한 결과(대상 demo.md r2 → r3)이지 도출 규칙의 변경이 아니다. 본 Pass는 CP2 독립 판정이며, 최종 승인(CP3)·재량 판정은 Advisor 소관으로 남는다. §8.3이 기록한 L-07 유형 재발은 Learn 단계의 신규 Lesson 후보 입력이다 (03 §3.2-C — 등록·승격은 Advisor 심사 소관).

---

## §5. verifier_scope (검사한 범위 / 검사하지 못한·제외한 범위)

**검사한 범위:**

- 산출물 문서 9종 **전문 정독** (표본 아님) + 물리 실데이터 22파일 **전문 판독** (store 21 JSON 전문 + index.jsonl 21라인 전문).
- VT-5 재관측·재계산: store↔index 전수 대조 / 스키마 42건 셀 단위 / content 21건 ↔ 05 스키마 / promotion-review 확정값 18건 전 필드 원문 대조 / 회수 재현 3케이스(ⓐ·ⓑ-4·ⓔ) index 기반 독립 재계산 / 재발 진리표 3케이스 / 음성 케이스 store 구성 역산 / 파일 ctime·mtime 밀리초 실측 / Lesson 후보 출처 §(handoff v0.1 §5.3·v0.2 §1.5·v0.3 §1.5) 실재·건수(1+3+4=8) 재열거.
- VT-4 전수 스캔: framework/memory 4문서 + structure.md 전문, 후보 5부류(§3.3 명시) 전체.
- VT-3 정합 대조: 필수/선택 표기 셀 단위(04·05·01 §3.2 원문 대비), contract id 3중 정적 대조, § 포인터 사슬, §9 이력 9문서, 부분 개정 잔여(r2·r3 개정 참조 지점 전수), 절차서 §2 인용 문장 ↔ ROADMAP·04 §7·05 §7 원문.
- 경계: mtime 타임라인(specs 15·v0.3 7·v0.4 전체), v0.3 크기 차이 원인 실측(Baseline 스탬프), 저장소 전역 데이터 파일 위치 검색, framework/ 전 디렉터리 인벤토리.

**검사하지 못했거나 제외한 범위 (V4):**

1. **형태 A "모든 접근 Port 경유"의 전칭 직접 관측**: Bootstrap(규약 실현) 특성상 시연 수행 중의 접근 행위 자체는 사후 관측 불가. 물리 결과 정합(store↔index 전수 일치·구성 완전 계정·경계 밖 데이터 0·여분 파일 0)과 문서 접근 서술 전수로 대체 검증했다. 우회가 있었다면 남을 물리 신호(고아 파일·정합 붕괴·경계 밖 데이터)는 0건이다.
2. **거부 케이스(ⓑ 1~3·ⓒ 음성)의 관측 자체**: 기대된 거부는 물리 흔적을 남기지 않는다(기록 미수행이 정상). 프로토콜의 결정성(memory-service §5.2 단계 1·2, lessons §3.2 승격 3조건은 입력만으로 판정 확정)으로 절차서 입력에 대한 결과를 독립 재도출해 기록과 대조했고, ⓒ는 store 구성 역산으로 보강했다.
3. **M7 임시 생성기·러너 스크립트**(세션 스크래치, 저장소 밖): 실물 검사 불가·불필요 — 판정은 스크립트가 아니라 물리 산출물 자체로 했다 (06 V1).
4. **Frozen spec(01·02·03·04·05·06·07·Glossary)·v0.3 Baseline 산출물 내용의 재검증**: 범위 외 — 정본·기준선으로 전제하고 대조 기준으로만 사용했다.
5. **docs/·adapters/ 경계의 금지 토큰 판정**: 제외 — C-3(및 확장)의 적용 대상이 아니다. memory-binding.md는 격리 보유 지점(위임 done 명시 제외), docs/는 운용 문서(절차서 자신의 "토큰 절제"는 자기 선언이며 계약 기준 아님).
6. **§ 포인터 대조의 잔여**: 동일 §의 반복 인용은 최초 1회 대조로 갈음했다. 상이한 §를 가리키는 참조는 전수 대조했다.
7. **specs/ "무변경"의 내용 수준 증명**: v0.4 이전 콘텐츠 스냅샷이 없어 mtime 증거(08:07, v0.4 구간 쓰기 0건)로 판정했다. 저장소가 버전 관리되지 않아 diff 불가 — mtime 위조 가능성은 검사 범위 밖이나 반증 신호 0건.

**재검증(r3)에서 추가로 검사한 범위:** demo.md r3 전문 재정독 · timestamp·시각 관련 상태 서술 전 지점 전수 재대조(잔존 리터럴 4곳의 라이브/이력 판별 포함) · memory-data/ 22파일 크기·mtime 밀리초 재실측 · demo.md §9 이력 r1·r2 행 문면 보존 대조 · 타 산출물 8종 크기·mtime 재실측 · 6시연 (E) 기록·§8·§10·§11 문면 r2 대비 전문 대조 · 교정 서술 ↔ 04 §3.2-A·05 §3.2-D·lessons.md §5.2 정합 대조. **재검증에서 재수행하지 않은 것:** 무변경이 실측 확인된 물리 데이터·타 산출물 8종에 대한 기존 충족 30건의 전면 재수행(§3.7-B·D의 무변경 실측으로 갈음 — Advisor 재검증 요청 범위와 일치).

---

## §6. rework (재작업 지시 — 06 §3.2-D)

**현재 상태 (재검증 후): 없음** — final_verdict = Pass (06 §3.2-A: Pass면 "없음").

이행 기록: 최초 판정(Fail)이 발행한 아래 재작업 지시는 demo.md **r3**(Worker, §9 이력 r3 행)로 이행되었고, Verifier가 revalidation_criteria 전 항목을 재수행해 해소를 확인했다 (§3.7). Verifier는 산출물을 수정하지 않았다 (06 INV-6).

| 필드 | 내용 (최초 판정 발행분 — 이행 완료) | 이행 확인 (§3.7) |
|---|---|---|
| `violated_items` | items #31 — (checklist §5.3 준용·06 §3.2-F) demo.md 머리 수행 일시 행의 "(Record 순서 단조 증가, **실제 수행 시각**)" 서술 ↔ 실측 모순: 기록 내 timestamp(22:19:47.602~22:20:07.602, 1초 간격)는 물리 파일 생성(전 21건 22:19:46.604~46.628 일괄)보다 미래의 생성기 배정 값으로, 각 기록의 실제 기록 시각이 아니다. | r3에서 해소 — 라이브 주장 2지점(head·§10) 교정 + §1.1 성격 명기 절 신설 + §8.3 결함·교정·L-07 재발 기록. 라이브 실측 모순 서술 잔존 0건 (§3.7-A). |
| `expected_state` | demo.md의 timestamp 관련 서술이 실측과 정합. 권장 최소 경로 **(a) 서술 교정**: timestamp = 생성기 배정 단조 스케줄 값·물리 기록은 22:19:46 일괄·04 계약 기능 영향 없음(시각 원천·포맷 Adapter 소관) 취지로 교정 + §9 r3 행 append. 물리 데이터 무수정(04 INV-6 — 데이터 자체는 결함 아님). 대안 (b) 실측 시각 재생성은 전면 재검증 필요로 비권장. | 달성 — 권장안 (a) 그대로 이행됨. 물리 데이터 무수정 실측(§3.7-B). |
| `revalidation_criteria` | (1) timestamp·시각 서술 전 지점 전수 열거·재대조 — 모순 0건·"실제 수행 시각" 류 잔존 0건(L-06 적용, 열거 목록 보고). (2) 물리 데이터 무변경 실측. (3) §9 이력 append-only 행 추가. (4) 타 산출물 8종 무변경 실측. (5) 교정 서술의 04 §3.2-A·05 §3.2-D 정합. | 5건 전부 충족 (§3.7-A/B/C/D/E — r3 이력 행이 교정 2지점 열거 명시). items #31 충족 전이 → final_verdict = Pass 재도출 (§4). |
| `evidence_gap` | 없음 (판정 불가 항목 0건). | 해당 없음. |

관찰 2건(§3.4 — 위반 아님, 재작업 비대상, 차기 개정 후보): (1) 재발 레코드 `verdict`·`matched_lesson_id`의 labels 투영을 lessons.md §5.2에 명시 편입, (2) structure.md §8 트리의 v0.4 파일 반영.

---

## §7. 요약 (한눈에 보기)

- 문서 9종 전문 정독 + 물리 실데이터 22파일 전문 판독·독립 재계산. **최초 판정: 충족 30 / 위반 1 / 판정 불가 0 → Fail** — 위반 1건은 demo.md의 timestamp "실제 수행 시각" 서술 ↔ 실측 모순(전 21건 22:19:46 일괄 생성, 기록 값은 +1~+21s 미래의 생성기 배정 값 — L-07 유형). 계약 기능·시연 실질 관측은 무손, 위반은 서술 계층 1건에 국한. 재작업 지시 발행(§6).
- **재검증 (demo.md r3 재작업 후): revalidation_criteria 전 항목 재수행 — 위반 1건 해소 확인 (§3.7). 충족 31 / 위반 0 / 판정 불가 0 → final_verdict = Pass** (06 §3.2-C 결정적 도출). rework = 없음. 물리 데이터 22파일·타 산출물 8종 무변경 실측.
- 충족 근거: VT-1 전수 실재 · ROADMAP R-1~R-4 · 04 §7 6건(contract id 3중 정적 대조 포함) · 05 §7 7건 전부 — 물리 재검증으로 성립: store 21↔index 21 전수 대조 일치, 스키마 42건 셀 단위 위반 0, Index Entry content 0건, promotion-review 확정값 전건 개작 0, 회수 재현 재계산 3케이스 일치({mi-0014} 최소 집합 1/8 포함), 재발 진리표 3/3, 음성 케이스 물리 파일 0 역산.
- VT-4 전수 스캔(후보 5부류): framework/memory 4문서 + structure.md 금지 토큰 실증 0건 (매치는 프로젝트 문서 파일명 참조뿐). 물리 데이터는 Adapter 경계 이하에만 존재, v0.3 산출물·specs/ 무변경(mtime 타임라인 + Baseline 스탬프 해명), 무단 산출물 0.
- 본 Pass는 CP2 독립 판정이다. 최종 승인(CP3)은 Advisor 소관. §8.3의 L-07 유형 재발 기록은 Learn 단계 신규 Lesson 후보 입력으로 남는다. 비차단 관찰 2건(§3.4 — 재발 레코드 투영의 lessons.md 명시 편입·structure.md §8 트리 갱신)은 차기 개정 후보.
