{"basis": {"gateEventRef": "gate-unit-impl-plan-m3-2-stt-m3-brief-md", "proposingStepRef": "impl-plan-m3-2-stt-m3-brief-md"}, "kind": "task_added", "payload": {"capability": "cap-impl-m3-silence-gate", "delegation": {"constraints": "워크스페이스(yt-stt) 밖 파일 생성·수정 금지, 보호 경계(.claude/·docs/solution-design.md) 읽기 전용", "context": ["Project Contract v4 architectureDirection.decisions [v1 계승] 무음 실패 게이트 원칙", "M3-BRIEF.md §3.1 — M1 다운믹스 무음 신호를 소비하는 하류 코드 0건 실측(강제 신설 근거)", "파일 존재 의존 금지 — stages 신호만 판독(§3.1)"], "done": "위 done 필드와 동일", "from": "Advisor", "input": ["scripts/m1/meta_schema.py (update_stage — 읽기 전용 import)", "scripts/m1/downmix_gate.py (stages.downmix 기록 형식 참조 — 읽기 전용)", "docs/requirements-def.md FR-12(a)(b)", "docs/functional-spec.md FS-12", "docs/interface-spec.md §2.5", "docs/table-def.md §5 (stages 필드)", "M3-BRIEF.md §3.1"], "output": ["scripts/m3/silence_gate.py"], "task": "위 task 필드와 동일", "to": "Worker"}, "dependsOn": [], "done": "python -c \"\nimport sys, os\nsys.path.insert(0, os.path.join('scripts', 'm3'))\nimport silence_gate as sg\n\nok = sg.check_entry_gate({'stages': {'downmix': {'status': 'ok', 'selected': 'mono'}}})\nassert ok == {'passed': True, 'reason': None, 'source': 'entry-gate'}, ok\n\nfailed_silent = sg.check_entry_gate({'stages': {'downmix': {'status': 'failed', 'reason': 'silent-after-downmix'}}})\nassert failed_silent['passed'] is False, failed_silent\nassert failed_silent['reason'] == 'silent-after-downmix', failed_silent\n\nfailed_measure = sg.check_entry_gate({'stages': {'downmix': {'status': 'failed', 'reason': 'downmix-measurement-failed'}}})\nassert failed_measure['passed'] is False, failed_measure\nassert failed_measure['reason'] == 'downmix-measurement-failed', failed_measure\n\nmissing = sg.check_entry_gate({'stages': {}})\nassert missing['passed'] is False, missing\nassert missing['reason'] == 'downmix-status-missing', missing\n\nno_stages = sg.check_entry_gate({})\nassert no_stages['passed'] is False, no_stages\n\nprint('OK')\n\"", "id": "m3-silence-gate", "interfaceContract": {"consumes": ["scripts/m1/meta_schema.py (update_stage — 읽기 전용 import)", "scripts/m1/downmix_gate.py (stages.downmix 기록 형식 참조 — 읽기 전용)", "docs/requirements-def.md FR-12(a)(b)", "docs/functional-spec.md FS-12", "docs/interface-spec.md §2.5", "docs/table-def.md §5 (stages 필드)", "M3-BRIEF.md §3.1"], "produces": ["scripts/m3/silence_gate.py"]}, "model": "sonnet", "ownedBoundary": ["scripts/m3/silence_gate.py"], "role": "Worker", "task": "너는 yt-stt 프로젝트 M3(2차 음성 STT 층) 구현 task 중 하나를 수행하는 fresh-context 실행 단위다. 정본 명세 = 워크스페이스 루트 `M3-BRIEF.md`(읽기 전용) §3.1 — 이 절과 아래 근거 문서를 Read로 정독한 뒤 구현하라. `M3-BRIEF.md`는 재정의 0 문서이며 Contract v4·Projection이 정본이다(충돌 시 그쪽이 우선하며, 발견되는 충돌은 완료 보고의 open_questions에 남겨라).\n\n[근거 — 전부 읽기 전용]\n- `docs/requirements-def.md` FR-12(a)(b) — 무음 실패 게이트 요구·수용 기준\n- `docs/functional-spec.md` FS-12 — 무음 실패 게이트 기능 책임\n- `docs/interface-spec.md` §2.5(F1 무음 실패 게이트) — \"빈/무음 결과... 처리 속도·산출 밀도 비정상을 성공이 아닌 실패로 세운다\"\n- `docs/table-def.md` §5 meta.json `stages` 필드 — \"무음 실패 게이트 데이터 훅\"(판정 로직은 하류 소관, 데이터 모델은 자리만 예약)\n- `.claude/project-contract/project-contract.v4.md` architectureDirection.decisions [v1 계승] \"무음 실패 게이트\" 원칙 인용 — 읽기만 하고 수정 금지\n- `scripts/m1/meta_schema.py`의 `update_stage(meta, stage_name, status, extra)` — `meta['stages'][stage_name] = {'status': ..., **extra}` 구조를 읽기 참조만 하라(수정 금지)\n- `scripts/m1/downmix_gate.py`의 `produce_audio16k` — `stages.downmix` 기록 예시: 실패 시 `{'status': 'failed', 'reason': 'silent-after-downmix'|'downmix-measurement-failed', 'selected': ...}`, 성공 시 `{'status': 'ok', 'selected': ...}`. **이 파일은 읽기 참조만 하고 수정하지 마라.**\n\n[문제 배경] `uaf-verified:`(M3-BRIEF §3.1) M1이 이미 내는 무음 판정 신호(`meta.stages.downmix.status`)를 소비하는 하류 코드가 현재 0건이다. STT 진입 전에 이 신호를 강제로 판독하는 게이트가 신설되어야 한다(강제 신설 — FS-12·FR-12).\n\n[산출] `scripts/m3/silence_gate.py` 1개 신규 생성.\n\n[구현 요건]\n`check_entry_gate(meta: dict) -> dict` 함수를 작성하라. `meta`는 이미 로드된 meta.json dict다(파일 경로가 아니라 dict를 받는다 — 파일 IO는 이 함수 책임이 아니다).\n- `meta.get('stages', {}).get('downmix', {}).get('status')`를 판독한다.\n- `status == 'failed'`이면 `{'passed': False, 'reason': meta['stages']['downmix'].get('reason'), 'source': 'entry-gate'}`를 반환한다 — STT를 실행하지 않고 실패로 세운다는 신호일 뿐, 이 함수 자체는 STT를 호출하지 않는다(호출자인 pipeline이 이 신호로 분기한다).\n- `status == 'ok'`이면 `{'passed': True, 'reason': None, 'source': 'entry-gate'}`를 반환한다.\n- `status`가 없거나(`stages`/`downmix` 키 자체가 없음) 인식 불가 값이면, **판단 안 서면 안전한 쪽**(Contract v4 quality \"사양 자동 감지 단순화\" 원칙과 동형의 보수 원칙 — 오교정 0 비대칭) 으로 `{'passed': False, 'reason': 'downmix-status-missing', 'source': 'entry-gate'}`를 반환한다.\n- **파일 존재로 판단하지 마라**(`audio16k.wav` 물리 유무는 신호가 아니다 — M3-BRIEF §3.1 \"파일 존재 의존 금지\"). 오직 `stages` 신호만 본다.\n- 이 함수는 순수 함수다(디스크 IO·네트워크 없음).\n\n[금지] 워크스페이스(yt-stt) 밖 파일 생성·수정 금지. `.claude/**`·`docs/solution-design.md`·`docs/*.md`(Projection 7종)·`scripts/m0/`·`scripts/m1/`·`scripts/m2/`·`fixtures/`·`tests/baseline/`·`out/`(기존 산출물) 무수정(import 재사용은 허용). 이 task는 `scripts/m3/silence_gate.py` 외 어떤 파일도 만들지 않는다.\n\n[CP1 자체 점검] Execute 종료 시 `done` AC를 실제 실행해 종료 코드 0을 확인한 뒤에만 완료 보고하라.", "unitType": "implementation"}, "revisionSeq": 1}
{"basis": {"gateEventRef": "gate-unit-impl-plan-m3-2-stt-m3-brief-md", "proposingStepRef": "impl-plan-m3-2-stt-m3-brief-md"}, "kind": "task_added", "payload": {"capability": "cap-impl-m3-completion-gate", "delegation": {"constraints": "워크스페이스(yt-stt) 밖 파일 생성·수정 금지, 보호 경계(.claude/·docs/solution-design.md) 읽기 전용", "context": ["M3-BRIEF.md §3.5 — 완료 후 밀도/속도 게이트는 전혀 미구현 상태였음(강제 신설 근거)", "M3-BRIEF.md §9 — 정밀 임계값은 FS-12 구현 미결이므로 명백한 극단만 게이트(발명 금지)", "위상반전 무음 사고(180초를 3.6초에 처리·0줄)의 직접 방어"], "done": "위 done 필드와 동일", "from": "Advisor", "input": ["docs/requirements-def.md FR-12(a)", "docs/functional-spec.md FS-12", "docs/interface-spec.md §2.5", "docs/table-def.md §5 (stages 데이터 훅)", "scripts/m0/stt_risk12_probe.py (임계 상수 선례 — 읽기 참조만)", "M3-BRIEF.md §3.5, §9"], "output": ["scripts/m3/completion_gate.py"], "task": "위 task 필드와 동일", "to": "Worker"}, "dependsOn": [], "done": "python -c \"\nimport sys, os\nsys.path.insert(0, os.path.join('scripts', 'm3'))\nimport completion_gate as cg\n\nempty = cg.check_completion_gate([], 180.0, 3.6)\nassert empty['passed'] is False, empty\nassert empty['reason'] == 'empty-output', empty\n\nabnormal = cg.check_completion_gate([{'start': 0.0, 'end': 1.0, 'text': 'x'}] * 5, 180.0, 3.6)\nassert abnormal['passed'] is False, abnormal\nassert abnormal['reason'] == 'abnormal-speed', abnormal\nassert abnormal['realtimeMultiplier'] > 40, abnormal\n\nnormal = cg.check_completion_gate([{'start': float(i), 'end': float(i + 1), 'text': 'hello'} for i in range(60)], 180.0, 190.0)\nassert normal['passed'] is True, normal\nassert normal['lineCount'] == 60, normal\nassert 0.5 < normal['realtimeMultiplier'] < 2.0, normal\n\nzero_elapsed = cg.check_completion_gate([{'start': 0.0, 'end': 1.0, 'text': 'x'}], 180.0, 0.0)\nassert zero_elapsed['passed'] is False, zero_elapsed\nassert zero_elapsed['reason'] == 'non-positive-elapsed', zero_elapsed\n\nprint('OK')\n\"", "id": "m3-completion-gate", "interfaceContract": {"consumes": ["docs/requirements-def.md FR-12(a)", "docs/functional-spec.md FS-12", "docs/interface-spec.md §2.5", "docs/table-def.md §5 (stages 데이터 훅)", "scripts/m0/stt_risk12_probe.py (임계 상수 선례 — 읽기 참조만)", "M3-BRIEF.md §3.5, §9"], "produces": ["scripts/m3/completion_gate.py"]}, "model": "sonnet", "ownedBoundary": ["scripts/m3/completion_gate.py"], "role": "Worker", "task": "너는 yt-stt 프로젝트 M3(2차 음성 STT 층) 구현 task 중 하나를 수행하는 fresh-context 실행 단위다. 정본 명세 = 워크스페이스 루트 `M3-BRIEF.md`(읽기 전용) §3.5·§9 — 이 절과 아래 근거 문서를 Read로 정독한 뒤 구현하라.\n\n[근거 — 전부 읽기 전용]\n- `docs/requirements-def.md` FR-12(a) — 무음 실패 게이트 수용 기준\n- `docs/functional-spec.md` FS-12 — \"완료 후 밀도/속도 게이트\" 기능 책임\n- `docs/interface-spec.md` §2.5 — \"처리 속도·산출 밀도 비정상(예: 180초를 3.6초에·0줄)을 성공이 아닌 실패로 세운다\"\n- `docs/table-def.md` §5 meta.json `stages` — \"무음 실패 게이트 데이터 훅... 비정상(180초를 3.6초에 \"처리\"·0줄 산출)을 성공이 아닌 실패로 세운다\"\n- `scripts/m0/stt_risk12_probe.py`의 `SILENT_FAILURE_REALTIME_MULTIPLIER = 50.0`·`MIN_OUTPUT_LINES_PER_MINUTE = 1.0` 상수와 그 주석 — **읽기 참조만**(import·수정 금지). \"180/3.6=50x\" 예시치를 이미 구현한 선례로만 참고하라. 정밀 임계값은 FS-12 \"구현\" 미결(M3-BRIEF §9 `uaf-assumed:`)이므로 이 task는 그 선례와 자릿수가 다른(명백한) 극단만 게이트하는 **보수적** 임계를 스스로 정의한다(정상 하한을 발명하지 않는다).\n\n[범위] 이 게이트는 **완료 후**(STT 실행이 끝난 뒤) 세그먼트 산출 결과를 점검한다. 진입 전 게이트(무음 판정 소비)는 별도 task(`m3-silence-gate`) 소관이며 이 task 범위가 아니다.\n\n[산출] `scripts/m3/completion_gate.py` 1개 신규 생성.\n\n[구현 요건]\n`check_completion_gate(segments: list, duration_sec: float, elapsed_sec: float) -> dict` 함수를 작성하라.\n- `segments`는 whisper 세그먼트 dict 리스트(`{'start':float,'end':float,'text':str,...}`, table-def §7.6)를 흉내낸 리스트다. 이 함수는 whisper를 호출하지 않는다 — 이미 나온 세그먼트 결과만 검사한다.\n- `line_count = len(segments)`. `line_count == 0`이면 `{'passed': False, 'reason': 'empty-output', 'lineCount': 0, 'realtimeMultiplier': None}`을 반환한다(0줄 = 실패·FS-12).\n- `elapsed_sec`가 0 이하이면 0으로 나누지 말고 `{'passed': False, 'reason': 'non-positive-elapsed', 'lineCount': line_count, 'realtimeMultiplier': None}`을 반환한다.\n- 그 외에는 `realtime_multiplier = duration_sec / elapsed_sec`를 계산한다. **극단적 고속**(선례 50x와 자릿수가 다르지 않은 수준 — 예: 20x 이상 등, 정확한 상수는 위 M0 선례를 참고해 보수적으로 스스로 정하되 정밀 임계는 발명하지 않는다는 §9 원칙을 지켜라)이면 `{'passed': False, 'reason': 'abnormal-speed', 'lineCount': line_count, 'realtimeMultiplier': realtime_multiplier}`를 반환한다.\n- 그 외(줄 수 > 0·정상 배율 범위)는 `{'passed': True, 'reason': None, 'realtimeMultiplier': realtime_multiplier, 'lineCount': line_count}`를 반환한다.\n- 반환 dict에는 항상 `realtimeMultiplier`(계산 가능한 경우)·`lineCount` 키를 포함해 `meta.json stages.stt` 기록 근거로 쓸 수 있게 하라(§4(c)).\n- 순수 함수(디스크 IO·모델 로드 없음) — **faster-whisper를 import하거나 호출하지 마라.**\n\n[금지] 워크스페이스 밖 파일 생성·수정 금지. `.claude/**`·`docs/solution-design.md`·`docs/*.md`·`scripts/m0/`·`scripts/m1/`·`scripts/m2/`·`fixtures/`·`tests/baseline/`·`out/` 무수정(참조만). 이 task는 `scripts/m3/completion_gate.py` 외 어떤 파일도 만들지 않는다.\n\n[CP1 자체 점검] Execute 종료 시 `done` AC를 실제 실행해 종료 코드 0을 확인한 뒤에만 완료 보고하라.", "unitType": "implementation"}, "revisionSeq": 2}
{"basis": {"gateEventRef": "gate-unit-impl-plan-m3-2-stt-m3-brief-md", "proposingStepRef": "impl-plan-m3-2-stt-m3-brief-md"}, "kind": "task_added", "payload": {"capability": "cap-impl-m3-stt-engine", "delegation": {"constraints": "워크스페이스(yt-stt) 밖 파일 생성·수정 금지, 보호 경계(.claude/·docs/solution-design.md) 읽기 전용", "context": ["M3-BRIEF.md 개정 이력 v1→v2 — 이전 시도가 done AC에 실제 large-v3 실행을 요구해 900초 타임아웃 2회로 escalated. 이번 계획은 done을 스텁/합성 입력으로 재구성", "실제 large-v3 audio16k 실행 검증 = Advisor CP3 독립 실증 1회(엔진 밖) — M3-BRIEF §8", "힌트 유래 = 1차 팩트 용어 + 참고자료 어휘만(OCR 유래 소멸·묶음 사전 경유 금지) — Contract v4 functional[4]·[9]", "언어 자동 감지 기본, 'ko' 하드코딩 금지(QR-4)", "M3-BRIEF.md §3.4 v2→v3 개정(옵션 D) — elapsed 측정 책임이 pipeline wall-clock 에서 stt_engine.transcribe_audio 반환값으로 이동. v2 escalated 귀속(pipeline wall-clock + 즉시반환 스텁 → 거짓 abnormal-speed) 재발 방지"], "done": "위 done 필드와 동일", "from": "Advisor", "input": ["docs/requirements-def.md FR-4(a)-(d)", "docs/functional-spec.md FS-4", "docs/interface-spec.md §2 (전체)", "docs/table-def.md §4.2, §4.3, §7.2, §7.6, §5", "Contract v4 functional 요구사항(2차 음성 STT·묶음 관리 묶음 사전 경유 금지)·quality 요구사항(STT 모델 large-v3 확정·언어 자동 감지)", "out/m2-samples/terms.txt (존재 확인만)", "M3-BRIEF.md §3.3, §3.4, §4(a)(b)"], "output": ["scripts/m3/stt_engine.py"], "task": "위 task 필드와 동일", "to": "Worker"}, "dependsOn": [], "done": "python -c \"\nimport sys, os, inspect, json\nsys.path.insert(0, os.path.join('scripts', 'm3'))\nimport stt_engine as se\n\nsrc = inspect.getsource(se)\nq1 = chr(39)\nq2 = chr(34)\nneedle_single = 'language=' + q1 + 'ko' + q1\nneedle_double = 'language=' + q2 + 'ko' + q2\nassert needle_single not in src, 'ko hardcoded (single-quote form) found'\nassert needle_double not in src, 'ko hardcoded (double-quote form) found'\n\nd_gpu = se.select_device(4096.0)\nassert d_gpu['device'] == 'cuda', d_gpu\nd_cpu_low = se.select_device(512.0)\nassert d_cpu_low['device'] == 'cpu', d_cpu_low\nd_cpu_none = se.select_device(None)\nassert d_cpu_none['device'] == 'cpu', d_cpu_none\nassert d_cpu_none['reason'] == 'undetermined-fallback-cpu', d_cpu_none\n\nhint = se.build_hint('fact term1 term2', 'ref termA')\nassert hint and 'term1' in hint and 'termA' in hint, hint\nassert se.build_hint(None, None) is None\nassert se.build_hint('', '') is None\n\nclass StubModel:\n    def transcribe(self, audio_path, initial_prompt=None, language=None):\n        segs = [{'id': 0, 'seek': 0, 'start': 0.0, 'end': 2.0, 'text': ' hello'},\n                {'id': 1, 'seek': 200, 'start': 2.0, 'end': 4.0, 'text': ' world'}]\n        class Info:\n            language = 'en'\n        return iter(segs), Info()\n\nresult = se.transcribe_audio(StubModel(), 'fake.wav', hint='x', language=None)\nassert result['language'] == 'en', result\nassert len(result['segments']) == 2, result\nassert result['segments'][0]['id'] == 0, result['segments'][0]\nassert result['segments'][0]['seek'] == 0, result['segments'][0]\nassert 'elapsed' in result, result\nassert isinstance(result['elapsed'], float), result['elapsed']\nassert result['elapsed'] >= 0.0, result['elapsed']\n\nts1 = se.format_timestamp(65, False)\nassert ts1 == '[01:05]', ts1\nts2 = se.format_timestamp(3661, True)\nassert ts2 == '[01:01:01]', ts2\n\nraw = se.render_raw_txt(result['segments'], duration_sec=4.0)\nlines = [l for l in raw.splitlines() if l.strip()]\nassert len(lines) == 2, lines\nassert lines[0].startswith('[00:00]'), lines[0]\nassert 'hello' in lines[0], lines[0]\nassert (not raw) or ord(raw[0]) != 0xFEFF, 'BOM leaked'\n\nj = se.render_transcript_json(result['segments'])\nparsed = json.loads(j)\nassert parsed[0]['seek'] == 0, parsed\n\nprint('OK')\n\"", "id": "m3-stt-engine", "interfaceContract": {"consumes": ["docs/requirements-def.md FR-4(a)-(d)", "docs/functional-spec.md FS-4", "docs/interface-spec.md §2 (전체)", "docs/table-def.md §4.2, §4.3, §7.2, §7.6, §5", "Contract v4 functional 요구사항(2차 음성 STT·묶음 관리 묶음 사전 경유 금지)·quality 요구사항(STT 모델 large-v3 확정·언어 자동 감지)", "out/m2-samples/terms.txt (존재 확인만)", "M3-BRIEF.md §3.3, §3.4, §4(a)(b)"], "produces": ["scripts/m3/stt_engine.py"]}, "model": "sonnet", "ownedBoundary": ["scripts/m3/stt_engine.py"], "role": "Worker", "task": "너는 yt-stt 프로젝트 M3(2차 음성 STT 층) 구현 task 중 하나를 수행하는 fresh-context 실행 단위다. 정본 명세 = 워크스페이스 루트 `M3-BRIEF.md`(읽기 전용) §3.3·§3.4·§4(a)(b) — 이 절과 아래 근거 문서를 Read로 정독한 뒤 구현하라.\n\n[중요 — 실행 시간 규율(M3-BRIEF §8·개정 이력 v1→v2)] 이전 시도(run `impl-yt-stt-m3` v1)는 이 task의 `done`에 **실제 large-v3 STT 실행**(수 분+모델 로드)을 요구해 엔진 Execute 컨텍스트가 900초 타임아웃 2회 → escalated로 정지했다. 이번 계획은 그 결함을 제거한다 — **이 task의 `done`은 실제 faster-whisper 모델 로드·추론을 절대 포함하지 않는다.** 실제 large-v3 실행 검증은 Advisor CP3 몫(엔진 밖·이 task 범위 밖)이다. 프로덕션 코드 자체는 실제 `WhisperModel`을 호출하도록 작성하되, **`done` AC는 그 호출부를 스텁 객체·합성 입력으로 대체해 순수 로직만 검증**한다.\n\n[근거 — 전부 읽기 전용]\n- `docs/requirements-def.md` FR-4(a)~(d) — 2차 STT 요구·수용 기준\n- `docs/functional-spec.md` FS-4 — 2차 STT 기능 책임·입출력 방향\n- `docs/interface-spec.md` §2 전체 — 모델(large-v3 확정·turbo 조건부 폴백 §2.4)·힌트(§2.1 \"힌트 유래 = 1차 팩트 용어 + 참고자료 어휘·OCR 유래 소멸·묶음 사전 경유 금지\")·언어(자동 감지 기본)·장치(GPU 여유 VRAM 있으면 GPU 아니면 CPU 코어 스레드·판단 안 서면 CPU+고지)·실패처리(§2.5)·이식성(§2.6 RISK-11 open)\n- `docs/table-def.md` §4.2(타임스탬프 `[MM:SS]`/1시간 이상 `[HH:MM:SS]`·단일 형식·오름차순 단조·BOM 없는 UTF-8)·§4.3(layer 5값 정본 — 이 task 산출은 `voice`)·§7.2(`transcript.raw.txt` = STT 직후 무보정 원본·절대 변형 금지)·§7.6(`transcript.json` = whisper 세그먼트 손실 없는 구조 원본 — `start`/`end`/`text`/`id`/`seek` 등 부가 필드 무선별 보존)·§5(meta.json `sttModel`·`sttDevice`·`language` 필드)\n- `.claude/project-contract/project-contract.v4.md` functional 요구사항 중 \"2차 음성 STT — faster-whisper large-v3... 힌트 유래 = 1차 팩트(수동 자막) 용어 + 참고자료 어휘. OCR 유래 힌트는 소멸한다\"와 \"묶음 관리... 묶음 사전 경유 금지\" 항목, quality 요구사항 중 \"STT 모델 = large-v3 확정... 언어 자동 감지·지정 가능(한국어 하드코딩 제거)\" — 읽기만 하고 수정 금지\n- `out/m2-samples/terms.txt` — glossary 파생 힌트 후보(참고자료 어휘). 파일 존재만 확인하고 내용 소비 로직은 이 task가 만든다(빈 파일이어도 됨 — 빈 문자열 처리를 견고하게 하라)\n\n[산출] `scripts/m3/stt_engine.py` 1개 신규 생성.\n\n[구현 요건 — 순수 함수 위주로 설계해 몽키패치 없이도 대부분 오프라인 테스트 가능하게 하라]\n\n1. `select_device(free_vram_mb) -> dict` — 순수 함수. `free_vram_mb`가 `None`이면(판단 불가) `{'device': 'cpu', 'computeType': 'int8', 'reason': 'undetermined-fallback-cpu'}`(판단 안 서면 안전한 쪽 CPU+고지). 2048.0 이상이면 `{'device': 'cuda', 'computeType': 'float16', 'reason': 'gpu-available'}`. 그 미만(0 이상)이면 `{'device': 'cpu', 'computeType': 'int8', 'reason': 'insufficient-vram'}`(interface-spec §2.4 GT1030 2GB 실측 = GPU 이득 없음 사례와 동형).\n2. `detect_device() -> dict` — `select_device`를 감싸 실제로는 `nvidia-smi --query-gpu=memory.free ...`를 subprocess로 조회하되(실패 시 `None` 취급), 그 결과를 `select_device`에 넘긴다. **이 함수는 done AC가 직접 호출하지 않는다**(하드웨어 의존). done은 `select_device`만 검증한다.\n3. `build_hint(fact_text, terms_text, max_chars=2000) -> str | None` — `initial_prompt` 힌트 문자열을 만든다. 유래는 **오직 `fact_text`(1차 팩트=facts/subtitle.md 내용)와 `terms_text`(참고자료=terms.txt 내용) 둘뿐**이다 — 다른 유래(화면 OCR 등)를 받는 파라미터를 이 함수에 추가하지 마라(계약 위반 — OCR 유래 힌트는 소멸됐다). 둘 다 `None`/빈 문자열이면 `None`을 반환한다(힌트 없이 진행). 길이 상한(`max_chars`)을 넘으면 앞부분 위주로 잘라낸다(§9 \"힌트 길이 상한·구성은 하류 실측 이월\" — 정교화하지 말고 단순 절단만 하라).\n4. `transcribe_audio(model, audio_path, hint, language=None) -> dict` — **`language` 파라미터의 기본값은 반드시 `None`이다. 소스 코드 어디에도 `'ko'`를 언어 기본값/하드코딩으로 쓰지 마라**(QR-4 회귀·M3-BRIEF §8 \"언어 회귀\"). `model`은 `.transcribe(audio_path, initial_prompt=hint, language=language)`를 호출할 수 있는 아무 객체(duck typing — 실제로는 `faster_whisper.WhisperModel` 인스턴스, 테스트에서는 스텁 객체)를 받는다. **이 함수는 `model.transcribe(...)` 호출 구간(모델 로드는 제외 — 오직 전사 호출 자체)을 `time.perf_counter()` 전후차로 감싸 순수 STT 소요 시간(초·float)을 측정해 반환한다 — STT 실행 시간(elapsed) 측정 책임은 pipeline wall-clock 이 아니라 이 함수다(M3-BRIEF §3.4 v2→v3 개정·옵션 D).** `uaf-verified:` v2 에서 `pipeline_m3.py` 가 `transcribe_fn` 호출 전후를 자체 `time.time()`으로 감싸 elapsed 를 재측정했고, done AC 의 스텁이 즉시 반환하는 탓에 그 wall-clock elapsed 가 거의 0 이 되어 `completion_gate` 의 realtime 배율이 거짓으로 폭증(임계 20x 대비 관측치 40x 대)해 `m3-pipeline` Worker 가 escalated 했다(정직하게 sleep/clamp 위조를 거부하고 보고) — 이 함수가 elapsed 를 직접 측정·반환해 그 귀속을 STT 실행 주체로 옮기는 것이 이번 개정의 핵심이다. 반환은 `{'segments': [세그먼트 dict, ...], 'language': 감지/지정된 언어 코드, 'elapsed': <float, 초>}`(세 번째 키 신설). `model.transcribe(...)`가 반환하는 (segments_iterable, info) 튜플 형태(faster-whisper 관례)를 그대로 리스트/필드로 정규화하되, **세그먼트의 부가 필드(`id`·`seek` 등)를 무선별 보존**하라(table-def §7.6 — 선별 금지). **`elapsed` 는 실측값 그대로 반환하라 — sleep·clamp·고정값 등으로 위조하지 마라**(§6-1 무음 위장 없음과 동형의 정직성 원칙. 테스트 스텁이 즉시 반환하면 이 함수가 실제로 측정한 아주 작은 값(0 이상)을 그대로 돌려주면 되고, 그 값을 호출자가 그대로 소비하는 것이 계약의 핵심이지 이 함수가 인위적으로 \"그럴듯한\" 값을 만들어내는 것이 아니다).\n5. `load_model(model_size, device, compute_type)` — 실제 `faster_whisper.WhisperModel(model_size, device=device, compute_type=compute_type)`를 생성해 반환한다(프로덕션 경로 — done AC는 이 함수를 호출하지 않는다). OOM 등 예외 시 `model_size='turbo'`로 1회 폴백하고 폴백 여부를 알 수 있게 `(model, actually_used_model_size)` 튜플을 반환하라(R7 — 말없이 안 내려가기).\n6. `format_timestamp(seconds, use_hms) -> str` — table-def §4.2: `use_hms=False`면 `[MM:SS]`, `True`면 `[HH:MM:SS]`.\n7. `render_raw_txt(segments, duration_sec) -> str` — `duration_sec >= 3600`이면 전체 파일에 HH:MM:SS 형식을 **단일 적용**(§4.2 \"한 전사본 안에서는 형식 단일\"), 각 줄 `f\"{timestamp} {text}\"`. 세그먼트 자체가 이미 시간순(whisper 자연 산출)이므로 이 함수는 입력 순서를 그대로 신뢰해 렌더링만 한다. BOM 없는 문자열을 반환한다(파일 쓰기는 pipeline 소관 — 이 함수는 문자열만 반환).\n8. `render_transcript_json(segments) -> str` — 세그먼트 리스트를 **무선별로** `json.dumps(segments, ensure_ascii=False, indent=2)` 형태 문자열로 반환한다(손실 없는 구조 원본).\n\n[재사용 경계] M1/M2 기존 함수는 필요 시 import만 허용(수정 금지). M0 `scripts/m0/stt_risk12_probe.py`는 STT 호출 참조 구현으로 **읽기 참조만**(재사용 여부·재구현 여부는 자유이나 이 파일을 import하거나 수정하지 마라 — 표류 방지).\n\n[금지] 워크스페이스 밖 파일 생성·수정 금지. `.claude/**`·`docs/solution-design.md`·`docs/*.md`·`scripts/m0/`·`scripts/m1/`·`scripts/m2/`·`fixtures/`·`tests/baseline/`·`out/` 무수정. `language='ko'` 등 한국어 하드코딩 절대 금지. 이 task는 `scripts/m3/stt_engine.py` 외 어떤 파일도 만들지 않으며, `done` AC에서 실제 `faster_whisper.WhisperModel`을 로드하지 않는다(스텁 객체로 대체).\n\n[CP1 자체 점검] Execute 종료 시 `done` AC를 실제 실행해 종료 코드 0을 확인한 뒤에만 완료 보고하라.", "unitType": "implementation"}, "revisionSeq": 3}
{"basis": {"gateEventRef": "gate-unit-impl-plan-m3-2-stt-m3-brief-md", "proposingStepRef": "impl-plan-m3-2-stt-m3-brief-md"}, "kind": "task_added", "payload": {"capability": "cap-impl-m3-pipeline", "delegation": {"constraints": "워크스페이스(yt-stt) 밖 파일 생성·수정 금지, 보호 경계(.claude/·docs/solution-design.md) 읽기 전용", "context": ["M3-BRIEF.md §3.2 — 부분 팩트 구간 경계 표현은 미명세(구현조 판정으로 열어둠, 임의 확정 금지)", "M3-BRIEF.md §7 — _JlQOnnEwxc 픽스처 무촉(STT 대상 제외)", "M3-BRIEF.md §8 — 실제 large-v3 실행은 Advisor CP3 몫(엔진 밖), 이 task done은 스텁만 사용", "M2 선례(m2-pipeline)는 실 표본에 직접 썼으나 그 로직은 가벼운 VTT 파싱이었음 — M3는 동일 패턴을 따르지 않음", "M3-BRIEF.md §3.4·§3.5 v2→v3 개정(옵션 D) — elapsed_sec 은 stt_engine.transcribe_audio(=transcribe_fn) 반환값의 'elapsed' 키에서만 가져온다. pipeline 자체 wall-clock 재측정 금지(v2 escalated 재현 방지)"], "done": "위 done 필드와 동일", "from": "Advisor", "input": ["scripts/m3/silence_gate.py", "scripts/m3/completion_gate.py", "scripts/m3/stt_engine.py", "scripts/m1/meta_schema.py (load_meta/save_meta/update_stage — 읽기 전용 import)", "docs/requirements-def.md FR-4(a)", "docs/table-def.md §5, §7.2, §7.6", "M3-BRIEF.md §2, §3.2, §3.4, §3.5, §4, §7, §8"], "output": ["scripts/m3/pipeline_m3.py"], "task": "위 task 필드와 동일", "to": "Worker"}, "dependsOn": ["m3-silence-gate", "m3-completion-gate", "m3-stt-engine"], "done": "python -c \"\nimport sys, os, json, tempfile\nsys.path.insert(0, os.path.join('scripts', 'm3'))\nimport pipeline_m3 as p3\n\ndef make_video_dir(base, stages_downmix, facts_name, facts_text):\n    d = os.path.join(base, 'video')\n    os.makedirs(os.path.join(d, 'facts'), exist_ok=True)\n    meta = {'duration': 4.0, 'language': None, 'stages': {'downmix': stages_downmix}}\n    with open(os.path.join(d, 'meta.json'), 'w', encoding='utf-8') as f:\n        json.dump(meta, f)\n    with open(os.path.join(d, 'facts', facts_name), 'w', encoding='utf-8') as f:\n        f.write(facts_text)\n    with open(os.path.join(d, 'audio16k.wav'), 'wb') as f:\n        f.write(b'RIFF....WAVEfake')\n    return d\n\ndef stub_transcribe_ok(model, audio_path, hint, language=None):\n    # elapsed is deliberately realistic (near meta['duration']=4.0) even though\n    # this stub call itself returns in microseconds -- this is the regression\n    # check for the v2 defect (pipeline wall-clock timing the stub's near-zero\n    # real execution instead of consuming this returned 'elapsed' field).\n    return {'segments': [{'id': 0, 'seek': 0, 'start': 0.0, 'end': 2.0, 'text': ' hi'},\n                          {'id': 1, 'seek': 200, 'start': 2.0, 'end': 4.0, 'text': ' there'}],\n            'language': 'en', 'elapsed': 4.2}\n\ndef stub_transcribe_empty(model, audio_path, hint, language=None):\n    return {'segments': [], 'language': 'en', 'elapsed': 4.2}\n\nwith tempfile.TemporaryDirectory(dir=os.getcwd()) as base:\n    d_fail = make_video_dir(os.path.join(base, 'a'), {'status': 'failed', 'reason': 'silent-after-downmix'}, 'EMPTY-SET.md', 'empty')\n    calls = []\n    def guard(*a, **k):\n        calls.append(1)\n        return stub_transcribe_ok(*a, **k)\n    r_fail = p3.run_m3(d_fail, transcribe_fn=guard)\n    assert r_fail['status'] == 'failed', r_fail\n    assert not calls, 'STT must not be invoked when entry gate fails'\n    assert not os.path.exists(os.path.join(d_fail, 'transcript.raw.txt'))\n\n    d_ok = make_video_dir(os.path.join(base, 'b'), {'status': 'ok', 'selected': 'mono'}, 'EMPTY-SET.md', 'empty')\n    r_ok = p3.run_m3(d_ok, transcribe_fn=stub_transcribe_ok)\n    assert r_ok['status'] == 'ok', r_ok\n    raw_path = os.path.join(d_ok, 'transcript.raw.txt')\n    json_path = os.path.join(d_ok, 'transcript.json')\n    assert os.path.exists(raw_path) and os.path.exists(json_path)\n    raw_bytes = open(raw_path, 'rb').read()\n    assert raw_bytes[:3] != bytes([0xEF, 0xBB, 0xBF]), 'BOM in transcript.raw.txt'\n    with open(os.path.join(d_ok, 'meta.json'), encoding='utf-8') as f:\n        meta_after = json.load(f)\n    assert meta_after.get('sttModel'), meta_after\n    stt_stage = meta_after.get('stages', {}).get('stt', {})\n    assert stt_stage.get('status') == 'ok', meta_after\n    rtm = stt_stage.get('realtimeMultiplier')\n    assert rtm is not None and rtm < 20.0, stt_stage\n\n    d_empty = make_video_dir(os.path.join(base, 'c'), {'status': 'ok', 'selected': 'mono'}, 'EMPTY-SET.md', 'empty')\n    r_empty = p3.run_m3(d_empty, transcribe_fn=stub_transcribe_empty)\n    assert r_empty['status'] == 'failed', r_empty\n    assert not os.path.exists(os.path.join(d_empty, 'transcript.raw.txt'))\n\n    d_skip = make_video_dir(os.path.join(base, 'd'), {'status': 'ok', 'selected': 'mono'}, 'subtitle.md', '[00:00] hello')\n    calls2 = []\n    r_skip = p3.run_m3(d_skip, transcribe_fn=lambda *a, **k: calls2.append(1))\n    assert r_skip['status'] == 'skipped', r_skip\n    assert not calls2, 'STT must not be invoked when facts already cover full duration'\n\nprint('OK')\n\"", "id": "m3-pipeline", "interfaceContract": {"consumes": ["scripts/m3/silence_gate.py", "scripts/m3/completion_gate.py", "scripts/m3/stt_engine.py", "scripts/m1/meta_schema.py (load_meta/save_meta/update_stage — 읽기 전용 import)", "docs/requirements-def.md FR-4(a)", "docs/table-def.md §5, §7.2, §7.6", "M3-BRIEF.md §2, §3.2, §3.4, §3.5, §4, §7, §8"], "produces": ["scripts/m3/pipeline_m3.py"]}, "model": "sonnet", "ownedBoundary": ["scripts/m3/pipeline_m3.py"], "role": "Worker", "task": "너는 yt-stt 프로젝트 M3(2차 음성 STT 층) 구현 task 중 하나를 수행하는 fresh-context 실행 단위다. 정본 명세 = 워크스페이스 루트 `M3-BRIEF.md`(읽기 전용) §2(입력)·§3.2(대상 구간 결정)·§4(산출)·§7(보호 경계)·§8(AC 규율) — 이 절들과 아래 근거 문서를 Read로 정독한 뒤 구현하라.\n\n[중요 — 실행 시간 규율(M3-BRIEF §8·개정 이력 v1→v2, m3-stt-engine task와 동일 배경)] 이 task의 `done` AC는 **실제 large-v3 STT를 절대 실행하지 않는다** — 무겁고(수 분+모델 로드) fresh-context 실행 컨텍스트가 900초 타임아웃으로 정지한 선례가 있다(v1 반려 사유). **`stt_engine.transcribe_audio`를 몽키패치/스텁으로 대체한 합성 입력으로만 오케스트레이션 로직(게이트 분기·산출 렌더링·meta 필드 기록)을 검증**하라. 실제 위사(`YY_mbFY7y4E`)·TED(`kz-I5zIGbj4`) 표본에 실제 large-v3를 돌리는 것은 **Advisor CP3 몫(엔진 밖)**이며 이 task 범위가 아니다 — **이 task의 `done`은 `out/m2-samples/**` 아래 어떤 실 표본 파일도 읽거나 쓰지 않는다.** (M2 선례 `m2-pipeline`은 실 표본에 직접 썼지만, 그 로직은 가벼운 VTT 파싱이었다 — M3는 무거운 ML 추론이 개입하므로 같은 패턴을 그대로 따르지 않는다.)\n\n[임시 테스트 픽스처 규율] `done` AC가 합성 meta.json·facts를 다루려면 **워크스페이스 내부**의 임시 디렉터리만 써라 — 예: `tempfile.TemporaryDirectory(dir=os.getcwd())`(OS 기본 임시 디렉터리는 워크스페이스 밖이므로 쓰지 마라). 컨텍스트 매니저로 자동 정리해 흔적을 남기지 마라.\n\n[근거 — 전부 읽기 전용]\n- `docs/requirements-def.md` FR-4(a) — \"1차 미확정분\"이 STT 대상\n- `docs/table-def.md` §5(meta.json 필드: `downmix`·`sttModel`·`sttDevice`·`language`·`stages`)·§7.2(`transcript.raw.txt`)·§7.6(`transcript.json`)\n- M3-BRIEF §2 — 입력 계약: `audio16k.wav`(M1)·`meta.json`(`stages.downmix`·`duration`·`subtitleKinds`·`language`)·`facts/`(M2 — `facts/subtitle.md` 또는 `facts/EMPTY-SET.md`)·`terms.txt`(참고자료 힌트원)\n- M3-BRIEF §3.2 — 대상 구간 결정: **`facts/EMPTY-SET.md` 존재**(=1차 팩트 공집합) → **전 구간이 STT 대상**. **`facts/subtitle.md` 존재**(=1차 팩트 확정) → 팩트 미확정 구간이 대상이나, **`uaf-assumed:` 부분 팩트의 구간 경계 표현은 현재 미명세**다(M2 산출은 파일 이분이지 구간 목록이 아니다). **이 task는 그 미명세를 스스로 확정하지 않는다** — `facts/subtitle.md`가 존재하면(현재 M2 이분 모델상 전 구간이 팩트로 덮였다는 뜻이므로) STT 대상이 없다고 보아 **STT를 실행하지 않고 `{'status': 'skipped', 'reason': 'fact-covers-full-duration'}`로 기록**하라(임의 확정 금지 — 부분 팩트 케이스가 실제로 나타나면 Validating 게이트로 표면화될 문제이지 이 task가 지금 발명할 문제가 아니다).\n- M3-BRIEF §4(a)(b)(c) — 산출 3종: `transcript.raw.txt`(voice층)·`transcript.json`(whisper 세그먼트 구조 원본)·meta.json 필드 추가(`sttModel`·`sttDevice`·`language`·`stages.stt`)\n- M3-BRIEF §7 — 보호 경계: `_JlQOnnEwxc` 픽스처 폴더는 무촉(STT 대상 제외·`transcript.*`·`facts/` 생성 금지·`meta.json` 수정 금지)\n\n[산출] `scripts/m3/pipeline_m3.py` 1개 신규 생성.\n\n[구현 요건]\n`run_m3(video_dir: str, transcribe_fn=None, model_size='large-v3') -> dict` 함수를 작성하라.\n- `transcribe_fn`은 `stt_engine.transcribe_audio`와 같은 시그니처의 콜러블을 주입 받는 파라미터다(기본값 `None`이면 실제 `stt_engine.load_model` + `stt_engine.transcribe_audio`를 쓰지만, **테스트는 항상 이 파라미터로 스텁을 주입**한다 — 실제 모델 로드 경로를 만들되 done AC가 그 경로를 타지 않게 하는 것이 핵심).\n- `video_dir/meta.json`을 `scripts/m1/meta_schema.load_meta`로 읽는다(import만 허용, 수정 금지).\n- `silence_gate.check_entry_gate(meta)`를 호출한다. `passed`가 `False`이면 STT를 실행하지 않고(`transcribe_fn`을 호출하지 않고) `{'status': 'failed', 'reason': gate_result['reason'], 'stage': 'entry-gate'}`를 반환, `meta['stages']['stt'] = {'status': 'failed', 'reason': ...}`로 갱신 후 `save_meta`로 저장한다.\n- `passed`가 `True`이면 `facts/` 폴더를 확인한다. `facts/EMPTY-SET.md`가 있으면 전 구간 대상(`audio16k.wav` 전체). `facts/subtitle.md`가 있으면 위 [근거]에서 정한 대로 STT를 스킵하고 `{'status': 'skipped', 'reason': 'fact-covers-full-duration'}`를 반환한다(메타에도 `stages.stt = {'status': 'skipped', ...}` 기록).\n- 실행 대상이면: `stt_engine.select_device`로 장치 판단(`stt_engine.detect_device()`를 통해 VRAM 조회 후 판단), `stt_engine.build_hint(facts_text, terms_text)`로 힌트 구성(`out/.../terms.txt` 경로가 없으면 `None` 취급), `transcribe_fn(model, audio_path, hint, language=meta.get('language'))` 호출(주입 시 `model` 인자는 `None`이어도 되게 하라 — 스텁은 `model`을 안 씀). `transcribe_fn`의 반환은 `stt_engine.transcribe_audio`와 동일 계약 — `{'segments': [...], 'language': ..., 'elapsed': <float>}`이다(M3-BRIEF §3.4 v2→v3 개정·옵션 D). **`elapsed_sec` 은 이 반환값의 `'elapsed'` 키에서 그대로 가져오라 — pipeline 자신이 `time.time()`/`time.perf_counter()` 등으로 `transcribe_fn` 호출 전후를 감싸 별도 wall-clock 측정을 하지 마라.** `uaf-verified:` v2 에서 이 자체 wall-clock 측정이 즉시반환 스텁과 결합해 elapsed 가 거의 0 이 되었고, 그 결과 `completion_gate` 의 realtime 배율(임계 20x)이 거짓으로 폭증(관측치 40x 대)해 정상 스텁 입력조차 `abnormal-speed` 로 오판·escalated 됐다 — 동일 결함 재현 금지가 이번 개정의 핵심이다.\n- 결과 세그먼트를 `completion_gate.check_completion_gate(segments, meta['duration'], elapsed_sec)`로 검사한다(여기서 `elapsed_sec` 은 바로 위에서 `transcribe_fn` 반환값의 `'elapsed'` 키로부터 얻은 값이며, pipeline 이 별도로 측정한 값이 아니다). 실패하면 `stages.stt`에 실패로 기록하고 `transcript.*` 파일을 쓰지 않는다(§3.5 \"성공이 아닌 실패로 세운다\").\n- 통과하면 `stt_engine.render_raw_txt`/`render_transcript_json`으로 `transcript.raw.txt`·`transcript.json`을 `video_dir`에 UTF-8 without BOM으로 쓰고, `meta['sttModel']`·`meta['sttDevice']`·`meta['language']`·`meta['stages']['stt']`(밀도/속도 지표 포함)를 갱신해 `save_meta`로 저장한다.\n- 함수는 항상 상태 dict(`{'status': 'ok'|'failed'|'skipped', ...}`)를 반환한다.\n\n[재사용 경계] `scripts/m1/meta_schema.py`(load_meta·save_meta·update_stage)는 import만 허용, 수정 금지. `scripts/m3/silence_gate.py`·`completion_gate.py`·`stt_engine.py`(이 계획의 선행 3개 task 산출)를 그대로 import해 조합하라 — 재구현 금지.\n\n[금지] 워크스페이스 밖 파일 생성·수정 금지. `.claude/**`·`docs/solution-design.md`·`docs/*.md`·`scripts/m0/`·`scripts/m1/`·`scripts/m2/`·`fixtures/`·`tests/baseline/`·`out/`(기존 산출물, 특히 `out/m2-samples/**` 실 표본) 무수정. `_JlQOnnEwxc` 폴더는 이 함수의 `video_dir` 인자로도 절대 넘기지 마라(테스트에서조차). 이 task는 `scripts/m3/pipeline_m3.py` 외 어떤 파일도 만들지 않으며, `done` AC는 워크스페이스 내부 임시 디렉터리만 쓰고 실행 후 자동 정리한다.\n\n[CP1 자체 점검] Execute 종료 시 `done` AC를 실제 실행해 종료 코드 0을 확인한 뒤에만 완료 보고하라.", "unitType": "implementation"}, "revisionSeq": 4}
{"basis": {"gateEventRef": "gate-unit-impl-plan-m3-2-stt-m3-brief-md", "proposingStepRef": "impl-plan-m3-2-stt-m3-brief-md"}, "kind": "task_added", "payload": {"capability": "cap-milestone-m3-acceptance", "delegation": {"constraints": "워크스페이스(yt-stt) 밖 파일 생성·수정 금지, 보호 경계(.claude/·docs/solution-design.md) 읽기 전용", "context": ["이 milestone 통과가 Phase M3 경계 승인(CP3·Advisor 경계 승인) 게이트를 트리거한다", "M3-BRIEF.md §8 — 실제 large-v3 실행 검증은 이 milestone 통과 후 Advisor CP3가 엔진 밖에서 1회 수행(이 milestone은 대신하지 않음)", "M2 선례 GATE_RECORD_M2.md·gate_check_m2.py의 파일명·구조 관례를 계승(읽기·수정은 금지, 새 파일만 작성)"], "done": "위 done 필드와 동일", "from": "Advisor", "input": ["scripts/m3/silence_gate.py", "scripts/m3/completion_gate.py", "scripts/m3/stt_engine.py", "scripts/m3/pipeline_m3.py", "docs/project-plan.md 부록 A", "M3-BRIEF.md §6, §7, §8, §9"], "output": ["scripts/m3/gate_check_m3.py", "scripts/m3/GATE_RECORD_M3.md"], "task": "위 task 필드와 동일", "to": "Worker"}, "dependsOn": ["m3-silence-gate", "m3-completion-gate", "m3-stt-engine", "m3-pipeline"], "done": "python -c \"\nimport subprocess, sys, os\nr = subprocess.run([sys.executable, os.path.join('scripts', 'm3', 'gate_check_m3.py')])\nassert os.path.exists(os.path.join('scripts', 'm3', 'GATE_RECORD_M3.md'))\nassert os.path.getsize(os.path.join('scripts', 'm3', 'GATE_RECORD_M3.md')) > 0\nsys.exit(r.returncode)\n\"", "id": "m3-milestone-gate", "interfaceContract": {"consumes": ["scripts/m3/silence_gate.py", "scripts/m3/completion_gate.py", "scripts/m3/stt_engine.py", "scripts/m3/pipeline_m3.py", "docs/project-plan.md 부록 A", "M3-BRIEF.md §6, §7, §8, §9"], "produces": ["scripts/m3/gate_check_m3.py", "scripts/m3/GATE_RECORD_M3.md"]}, "model": "sonnet", "ownedBoundary": ["scripts/m3/gate_check_m3.py", "scripts/m3/GATE_RECORD_M3.md"], "role": "Worker", "task": "너는 yt-stt 프로젝트 **M3(2차 음성 STT 층) phase 경계 인수 판정** milestone 단위를 수행하는 fresh-context 실행 단위다. 정본 명세 = 워크스페이스 루트 `M3-BRIEF.md`(읽기 전용) §6(완결 판정 부록 A 인용)·§7(보호 경계)·§8(AC 규율) — 이 절들과 `docs/project-plan.md` 부록 A를 Read로 정독한 뒤 구현하라. 이 milestone 통과가 **Phase M3 경계 승인(CP3·Advisor 경계 승인)** 게이트를 트리거한다.\n\n[역할] 너는 선행 4개 구현 task(`silence_gate.py`·`completion_gate.py`·`stt_engine.py`·`pipeline_m3.py`)의 판단·렌더링 로직을 재구현하지 않는다. 그 산출을 import해 M3-BRIEF §6(완결 판정)·§7(보호 경계)·§8(AC 규율)이 요구하는 실증 기준을 실제로 만족하는지 판정한다.\n\n[중요 — 실행 시간 규율(M3-BRIEF §8, 다른 M3 task들과 동일 배경)] 이 milestone의 `done` AC도 **실제 large-v3 STT를 절대 실행하지 않는다.** 이전 시도(v1)가 이 원칙을 어겨 900초 타임아웃 2회로 escalated됐다. 여기서 하는 것은 **구조 확인 + 합성 입력 negative-control**뿐이다. 실제 위사·TED 표본에 실제 large-v3를 돌려 `transcript.raw.txt`·`transcript.json`을 검증하는 것은 **이 milestone 통과 후 Advisor가 CP3에서 별도로(엔진 밖) 1회 수행**한다 — 이 milestone은 그 실행을 대신하지 않는다.\n\n[근거 — 전부 읽기 전용]\n- `docs/project-plan.md` 부록 A — 마일스톤 완료 판정 원칙(오교정 0·타임스탬프 불가침·원본 불변·층 출처 명시·무음 실패 없음)\n- M3-BRIEF §6 — M3 특화 완결 판정 7항목(무음 위장 없음 최우선·오교정 0·타임스탬프 불가침·원본 불변·층 출처 명시·R7 모델/장치 기록·언어 하드코딩 부재)\n- M3-BRIEF §7 — 보호 경계: `_JlQOnnEwxc` 무촉\n- M3-BRIEF §8 — \"음성 대조(negative control)\"·\"진입 게이트 대조\"·\"언어 회귀\" 대조 항목을 오프라인·합성 입력으로 재확인\n\n[산출] `scripts/m3/gate_check_m3.py`(인수 판정 스크립트)·`scripts/m3/GATE_RECORD_M3.md`(판정 결과 기록) 2개 신규 생성.\n\n[구현 요건] `scripts/m3/gate_check_m3.py`는 실행 시(`if __name__ == '__main__':`) 아래를 전부 점검하고, 하나라도 실패하면 0이 아닌 종료 코드로 끝나며, 전부 통과하면 `scripts/m3/GATE_RECORD_M3.md`에 각 판정 항목과 결과(pass/fail)를 적어 쓰고(비어있지 않게) 종료 코드 0으로 끝난다.\n\n판정 항목:\n1. **모듈 임포트** — `scripts/m3/{silence_gate,completion_gate,stt_engine,pipeline_m3}.py` 4개 파일이 존재하고 문법 오류 없이 import 가능한가(무거운 최상위 실행이 없다는 방증 — import가 즉시 끝나야 한다).\n2. **진입 게이트 양방향 대조** — `silence_gate.check_entry_gate`에 합성 `stages.downmix.status='failed'`(사유 `silent-after-downmix`) meta를 넣으면 `passed=False`, `status='ok'` meta를 넣으면 `passed=True`인가(M3-BRIEF §8 \"진입 게이트 대조\").\n3. **완료 후 게이트 음성 대조** — `completion_gate.check_completion_gate`에 (a) 빈 세그먼트 리스트, (b) 비정상 고속(realtime 배율이 자릿수가 다른 수준) 합성 입력을 넣으면 둘 다 `passed=False`인가(M3-BRIEF §8 \"음성 대조\").\n4. **언어 하드코딩 부재** — `scripts/m3/stt_engine.py` 소스 텍스트에 `language='ko'`·`language=\"ko\"` 리터럴이 없는가(QR-4 회귀·소스 문자열 검색).\n5. **파이프라인 통합(합성)** — `pipeline_m3.run_m3`에 워크스페이스 내부 임시 디렉터리(`tempfile.TemporaryDirectory(dir=os.getcwd())`)와 몽키패치된 `transcribe_fn`으로 (a) 진입 게이트 실패 시 STT 미호출·(b) 정상 경로에서 `transcript.raw.txt`/`transcript.json` 생성·BOM 없음·`meta.json`의 `sttModel`/`stages.stt` 기록을 확인하는가.\n6. **보호 경계 무촉** — `out/m2-samples/The Kind of AI We Actually Need Van Jones TED [_JlQOnnEwxc]/`에 `transcript.raw.txt`·`transcript.json`이 여전히 없는가(이 milestone도, 앞선 4개 task도 이 폴더에 STT를 실행하지 않았음의 방증). 이 폴더의 `meta.json`을 읽기만 하고 절대 쓰지 마라.\n7. **실 표본 무촉** — `out/m2-samples/Why I Love My Bad Days TED [kz-I5zIGbj4]/`·`out/m2-samples/제휴마케팅 부업이 10년 넘게 블루오션인 이유 wisa [YY_mbFY7y4E]/`에도 아직 `transcript.raw.txt`가 없는가(실제 STT 실행은 CP3 몫 — 이 milestone 시점엔 아직 없어야 정상이다). 이 두 폴더의 `meta.json`을 읽기만 하고 절대 쓰지 마라.\n\n`scripts/m3/GATE_RECORD_M3.md`에는 위 7항목 각각의 이름과 pass/fail을 사람이 읽을 수 있게 적어라(M1/M2의 `GATE_RECORD_M1*.md`/`GATE_RECORD_M2.md` 관례를 참고하되 그 파일들을 읽거나 수정하지 마라 — 이 task는 그 파일명·형식 관례만 계승해 새 파일을 쓴다).\n\n[금지] 워크스페이스 밖 파일 생성·수정 금지. `.claude/**`·`docs/solution-design.md`·`docs/*.md`·`scripts/m0/`·`scripts/m1/`·`scripts/m2/`·`fixtures/`·`tests/baseline/`·`out/`(기존 산출물 전체, 특히 `_JlQOnnEwxc`·`kz-I5zIGbj4`·`YY_mbFY7y4E` 표본) 무수정. 이 task는 `scripts/m3/gate_check_m3.py`·`scripts/m3/GATE_RECORD_M3.md` 외 어떤 파일도 만들지 않는다.\n\n[CP1 자체 점검] Execute 종료 시 `done` AC를 실제 실행해 종료 코드 0을 확인한 뒤에만 완료 보고하라.", "unitType": "milestone"}, "revisionSeq": 5}
