# [시연 픽스처 — 실계약 문서 아님] fixture-t3-spec — 3-Task 병렬 집합 T3 (consumes 계약 Y)

> **시연 픽스처 — 실계약 문서 아님.** 이 파일은 v0.7 Workflow 시연(Task WF9, 3-Task 병렬 집합의 T3 — consumes 계약 Y)을 위해 구성된 **자기완결 대상 명세**다. specs/·framework/ 등 실계약 문서가 **아니다**. 이 파일과 그 산출물(out-t3.md)은 `docs/v0.7-demo-fixtures/` **내부에만** 존재한다 (v0.7-demo-procedure §4.1 격리, CP2 전수 스캔 제외 verifier_scope).
>
> **의도적 결함 정당 보유 — 본 파일 해당 없음.** 본 시연 픽스처 세트의 의도적 결함(정당 보유)은 충돌 유도 지점 1건으로 **fixture-t2-spec에만** 심겨 있다(§4.3). **T3 자기 파일에 심긴 결함은 없다** — 결함 없는 **통과 케이스**로, §A 작성 지시를 충실히 따른 **첫 산출로 §B done 전 항목이 충족 가능**하다(개별 CP2 Pass 설계). T3는 **확정 계약 Y 문면만 참조**하며, 동시 진행 중인 미완성 out-t2.md는 추측·인용하지 않는다(R2).

---

## §A. 대상물 · 산출 위치 (위임 대상 작업 — consumes 계약 Y)

확정 계약 Y 문면을 소비하여 **색인 사용 안내** 1건을 작성하라. out-t3.md에 (i) 아래 확정 계약 Y 문면을 그대로 **인용**하고, (ii) "색인은 가나다 순(감마 → 베타 → 알파)으로 정렬되어 있다"는 **사용 문장**을 두고, (iii) 용어 3개(감마·베타·알파) 각각의 **색인 참조 줄 3개**를 둔다.

- **산출 위치:** `docs/v0.7-demo-fixtures/out-t3.md` (후속 Task Worker가 생성).

## 계약 문면 (consumes: 확정 계약 Y 문면 — 인용)

> **확정 계약 Y 문면.** out-t2.md는 2열 표로 용어 3개를 **가나다 순(감마 → 베타 → 알파)**으로 나열한다.

## §B. 완료 조건 (done — 전 항목 예/아니오 판정 가능)

① out-t3.md에 확정 계약 Y 문면("out-t2.md는 2열 표로 용어 3개를 가나다 순(감마 → 베타 → 알파)으로 나열한다")의 **인용**이 존재한다.
② "색인은 가나다 순(감마 → 베타 → 알파)으로 정렬되어 있다"는 **사용 문장**이 존재한다.
③ 용어 3개(감마·베타·알파) 각각의 **색인 참조 줄 3개**가 존재한다.
④ 산출 위치가 `docs/v0.7-demo-fixtures/out-t3.md`다.
