# framework/workflow/merge-rules — 병합 연산·병합 결과·충돌 처리 규칙 인스턴스

작성일: 2026-07-06
상태: v0.7 Baseline (CP2 Pass — 첫 판정 Pass · CP3 승인 · 사용자 승인 2026-07-06)
상위 규약: AGENT.md
근거 정본:

- specs/07-workflow.md §3.1-C — Merge(병합) 연산(입력·출력·완료 조건 3건·실패 reason 3종)의 정본. 본 문서 §2가 인스턴스화하는 연산 계약. 입력·출력·완료 조건·실패의 진위 판정 기준은 이 §가 유지한다.
- specs/07-workflow.md §3.2-D — Merge Result(병합 결과) 4필드와 충돌 처리 순서 문면의 정본. 본 문서 §3이 4필드를 인스턴스화하고, §4가 순서 문면을 보존한 뒤 각 단계를 검사·처리 규칙으로 전개한다.
- specs/07-workflow.md §3.3 INV-5(검증 후 병합)·INV-6(중재자 = Advisor) — 본 문서 §5가 준수·대조하는 불변. INV-7(Memory 단일 Port)·INV-8(경계 불가침)·INV-9(AI 비의존)는 §6이 대조.
- specs/07-workflow.md §6 — 대표 실패 시나리오(미검증 결과 병합 → `UnverifiedResult`, 상호 참조 불일치 → `CrossReferenceMismatch` → Advisor 중재, 충돌 미해소 → `UnresolvedConflict` → Advisor 중재). 본 문서 §4 reason ↔ 완료 조건 결합의 대조 지점.
- specs/07-workflow.md §9 결정 기록 — "INV-5의 판정 기준은 06 §3.2-C 최종 판정 사용". 본 문서 §5가 § 포인터로만 준수한다(판정 기준 재정의 0).
- specs/07-workflow.md §3.1·§3.2-E·§3.1-A·§3.1-B — 연산 규칙 결합·공통 Failure Report 포맷·Decompose·Dispatch 연산. **이 파일 비소관 경계** — 공통 Failure Report 포맷은 work-graph.md § 포인터로 소비하고, Decompose·Dispatch는 별도 인스턴스 문서 소관이다(본 문서 §0·§6).
- specs/07-workflow.md §4.1·§4.2 — Adapter Binding(상호 참조 정합성·충돌 중재 행·중재 진입점)·이식 교체 지점. 병합 수합·중재 진입점 물리 채널의 소관 포인터(본 문서 §6).
- specs/06-verifier.md §3.2-C — 최종 판정 도출(통과/실패/조건부). "개별 검증을 통과했다 = 최종 판정 Pass"의 판정 기준·도출 규칙의 정본. 본 문서 §5가 § 포인터로만 참조하며 도출 규칙을 재서술하지 않는다.
- specs/06-verifier.md §3.2-A — 검증 리포트 스키마. 병합 선행 조건이 소비하는 판정 산출물의 정본. 소비 지점 포인터(재게재 0).
- specs/02-agent.md §3.2-C — 완료 보고 메시지(Merge 입력 동반물). §3.2-A 역할 경계(설계·역할 충돌의 결정 권한 = Advisor). 본 문서 §2·§5가 § 포인터로만 인용한다(재정의 0, 07 INV-3).
- framework/workflow/work-graph.md — **WF1 확정 형제**. Work Graph(07 §3.2-A)·Task(07 §3.2-B)·공통 Failure Report(07 §3.2-E) 포맷의 인스턴스 소유 문서. 본 문서는 그 세 포맷을 재게재하지 않고 저장소 문서 식별자 + § 포인터로만 소비한다(work-graph.md §0·§4 — 각 `reason`을 어느 연산이 언제 산출하는가는 규칙 인스턴스 문서 소관).
- framework/verifier/verification-report.md — 검증 리포트 스키마 확정 인터페이스(06 §3.2-A 인스턴스). 병합 선행 조건의 소비 지점 대조 포인터(재서술 0).
- framework/core/structure.md §2 — 본 파일의 소속 경계(Module 구현 디렉터리 `framework/{loop,memory,verifier,workflow,plugins}/` 중 `framework/workflow/`). §5 — 금지 토큰 규칙(확정 조건 C-3 확장). §0 정본 경계 관례.
- specs/00-glossary.md §3.2-J(J-07) — Work Graph·Task·병렬 집합·소유 경계·인터페이스 계약 용어 정본. 본 문서는 새 용어를 신설하지 않는다.
- 관례 표본: framework/verifier/verifier-protocol.md·framework/loop/loop-protocol.md — 연산을 운용 절차(검사·처리 규칙)로 전개하되 절차 단계 ↔ 정본 § 1:1 대응표를 두고 진위 판정 기준은 정본 §에 두는 형식. framework/workflow/work-graph.md — 같은 `framework/workflow/` 경계의 데이터 포맷 인스턴스 관례(문서 머리 구조·§9 이력 절 머리 배치·§0 정본 경계·§ 포인터 표기·말미 요약 절·자가 전수 스캔 기록).
- AGENT.md — 상위 규약.

거버넌스: 이 문서는 `framework/workflow/` 소속 Module 구현 디렉터리 문서다(framework/core/structure.md §2). 문서 본문은 특정 AI·언어·툴체인·직렬화 형식·물리 경로 비의존을 유지한다(framework/core/structure.md §5 C-3 확장, 07 §3.3 INV-9). 개정은 Advisor 승인 + 본 문서 §9 이력 절 기록으로만 이뤄진다(docs 운용 문서 거버넌스 관행 — session-handoff-v0.2 §1.3).

---

## §9. 이력 (Revision History)

| 일자 | 버전 | 변경 | 주체 |
|---|---|---|---|
| 2026-07-06 | v0.7 Draft | 최초 작성. Merge 연산(07 §3.1-C)의 입력·출력·완료 조건 3건·실패 reason 3종을 인터페이스 인스턴스로 전개(§2, 정본 그대로 보존 — 재정의·확장 0). Merge Result 4필드(07 §3.2-D)를 정본 보존 표로 인스턴스화(§3 — `crossRefStatus` 열거 Consistent/Mismatch·`arbitration` 중재자=Advisor 보존, 새 필드·새 열거 0). 충돌 처리 순서 문면(07 §3.2-D) 보존 + 5단계(수합/상호 참조 정합성 검증/충돌 검출/(충돌 시) Advisor 중재/병합 성립)를 예/아니오 판정 가능한 검사·처리 규칙으로 전개, 단계 ↔ Merge Result 필드 ↔ 완료 조건 ↔ 실패 reason 1:1 결합표(§4). INV-5·INV-6 준수 및 "개별 검증 통과 = 06 §3.2-C 최종 판정 Pass"를 § 포인터로만 참조(§5 — 07 §9 결정 기록 정합, 판정 기준·도출 규칙 재서술 0). 검증 리포트 스키마(06 §3.2-A·verification-report.md)·Work Graph·Task·공통 Failure Report 포맷(work-graph.md)은 § 포인터로만 소비(재게재 0). Decompose·Dispatch(07 §3.1-A/§3.1-B) 비소관, 검증 판정 기준 비정의(07 INV-8·Non-Goals — 06 소관), 동시 작성 형제(분해 규칙·디스패치 프로토콜) 불인용(07 R2), 물리 실현(수합·중재 진입점 채널) Adapter Binding 소관 포인터, 금지 토큰 0(자가 전수 스캔 — §6), Glossary 밖 새 용어 0. | Worker (Advisor 위임, Task WF5) |
| 2026-07-06 | v0.7 Baseline | v0.7 마일스톤 사용자 승인 — 기준선 확정 (CP2 Pass — 첫 판정 Pass·재작업 0회, 충족 29/위반 0/판정 불가 0; CP3 Advisor 승인). | Advisor |

(이력 절은 문서 머리에 둔다 — 거버넌스 추적 대상 문서 관행. 이후 개정은 이 표에 append-only로 기록한다.)

---

## §0. 이 문서의 위치와 정본 경계

- **정본은 specs/07-workflow.md §3.1-C·§3.2-D다.** 이 문서는 그 Merge 연산 계약(§3.1-C)과 Merge Result 포맷(§3.2-D)의 **인스턴스**이며, 연산·데이터 포맷·불변 규칙을 **재정의·확장하지 않는다**. 계약 요소는 07의 해당 §를 § 포인터로 참조한다(framework/core/structure.md §7 확정 조건 C-1 정합 — 인스턴스는 계약 변경이 아니다; verifier-protocol.md §0·loop-protocol.md §0과 동형).
- 이 문서는 `framework/workflow/` 아래 **병합 규칙**을 확정한다 — Merge 연산(입력·출력·완료 조건·실패), Merge Result 4필드, 충돌 처리 순서의 단계별 검사·처리 규칙. 07 §3.1-C·§3.2-D의 계약을 이 경계 위에 배치·대조한다. work-graph.md §0이 "07 §3.2-D 소관 별도 인스턴스 문서(병합 규칙)"로 지목한 자리가 본 문서다.
- **연산 규칙 인스턴스로서의 소유 경계.** work-graph.md는 공통 Failure Report 포맷(07 §3.2-E)의 **포맷 인스턴스 소유 문서**이며, 각 `reason` 코드를 어느 연산이 언제 산출하는가(연산별 완료 조건과 실패 보고의 결합)는 **연산 규칙 인스턴스 문서 소관**이라고 명시했다(work-graph.md §0·§4). 본 문서가 그 규칙 인스턴스 중 **Merge 연산**을 담당한다 — Merge의 3종 `reason`(`UnverifiedResult`/`CrossReferenceMismatch`/`UnresolvedConflict`)이 어느 완료 조건 미충족에서 산출되는가의 **결합**을 소유하되, 공통 Failure Report의 4필드 구조·9종 `reason` 전체 열거는 **재게재하지 않고** work-graph.md §4를 § 포인터로 소비한다.
- **이 문서는 Module 구현 디렉터리 문서다.** `framework/workflow/`는 structure.md §2가 지정한 Module 구현 디렉터리 경계이며, work-graph.md(WF1)가 이 경계의 첫 실사용 인스턴스다. 이 문서 본문은 특정 AI 이름·모델명·제품 기능명·언어명·툴체인명·직렬화 형식명·물리 경로·확장자를 두지 않는다(structure.md §5 C-3 확장, 07 §3.3 INV-9).
- **인스턴스화 대상 경계 — Merge만.** 본 문서의 인스턴스화 대상은 Merge 연산(07 §3.1-C)과 Merge Result(07 §3.2-D) 및 충돌 처리 순서다. **Decompose(07 §3.1-A)·Dispatch(07 §3.1-B) 연산은 이 파일 소관이 아니다** — 각각 별도 인스턴스 문서 소관이며, 본 문서는 그 내용을 인용·추측하지 않고 "07 §3.1-A/§3.1-B 소관" 수준의 일반 포인터로만 가리킨다(07 R2, §6).
- **검증 판정 기준 비정의(07 INV-8·Non-Goals).** "개별 검증을 통과했다"의 판정 기준(무엇이 통과인가)은 이 문서가 정의하지 않는다 — specs/06-verifier.md 소관이다(07 §3.1-C 주의, §9 결정 기록). 본 문서는 검증 통과 여부(최종 판정 Pass)를 병합의 **선행 조건**으로 요구·소비할 뿐이며, 판정 자체·도출 규칙을 재서술하지 않는다(§5). 상호 참조 정합성 판정(`crossRefStatus`)은 이와 구분되는 **Workflow 자기 소관**이다(07 §3.1-C 출력·§3.2-D — §4·§5).
- **위임 계약 불침범.** Merge 입력이 동반하는 완료 보고(02 §3.2-C)와 역할 경계(02 §3.2-A)는 02 소유이며 본 문서는 **§ 포인터로만 인용**한다(07 INV-3 — Workflow는 위임·역할 경계 포맷을 재정의하지 않고 인용한다).
- **물리 실현 비서술(경계).** 병합 결과 수합·상호 참조 대조·충돌 중재 진입점의 **물리 채널·직렬화·물리 위치**는 이 문서가 서술하지 않는다. 이는 **Adapter Binding 문서 소관**이다(07 §4.1 상호 참조 정합성·충돌 중재 행·§4.2 이식 교체 지점 — 중재 진입점). 필요한 자리에는 "Adapter Binding 문서 소관(07 §4.1·§4.2)" 포인터만 둔다(§6).
- **07·06 내부 § 포인터 표기 관례.** 07·06 정본 요소를 가리킬 때 그 내부 § 포인터는 `07 §…`·`06 §…` 접두로 명시하여 본 문서 자신의 절 번호와 구분한다. 열거 값 명칭(Merge / Consistent / Mismatch / 통과·Pass 등)과 불변 라벨(INV-5·INV-6, R1~R4)은 정본 명명 그대로 둔다.
- 용어는 specs/00-glossary.md 정본(§3.2-J J-07: Work Graph·Task·병렬 집합·소유 경계·인터페이스 계약)만 사용한다. 새 용어를 신설하지 않는다. "병합 결과·상호 참조 정합성·충돌·중재·수합" 등은 07 §3.1-C·§3.2-D 정본이 정의한 연산·포맷·서술 명칭이지 본 문서 신설 용어가 아니다.

---

## §1. 목적

`framework/workflow/`의 이 규격은 **완료·검증된 병렬 Task 결과를 어떻게 병합하고 충돌을 처리하는가**를 확정한다.

이 규격의 책임은 네 가지다.

- **Merge 연산 인터페이스** — Merge 연산(07 §3.1-C)의 입력(완료·검증된 Task 결과 집합)·출력(병합 결과 + 상호 참조 정합성 판정 + 충돌 목록)·완료 조건 3건·실패 reason 3종을 07 §3.1-C 정본의 인스턴스로 전개한다(§2).
- **Merge Result 포맷** — 병합 결과 4필드(`collected`/`crossRefStatus`/`conflicts`/`arbitration`)를 07 §3.2-D 정본 그대로 보존한 인스턴스 표로 확정한다(§3).
- **충돌 처리 규칙 전개** — 07 §3.2-D의 충돌 처리 순서(수합 → 상호 참조 정합성 검증 → 충돌 검출 → (충돌 시) Advisor 중재 → 병합 성립)를 보존하고, 각 단계를 **예/아니오 판정 가능한 검사·처리 규칙**으로 전개하며, 단계 ↔ Merge Result 필드 ↔ 완료 조건 ↔ 실패 reason의 결합을 확정한다(§4).
- **불변 준수와 판정 기준 참조 경계** — INV-5(검증 후 병합)·INV-6(중재자 = Advisor)를 준수하고, "개별 검증 통과 = 06 §3.2-C 최종 판정 Pass"를 § 포인터로만 참조하여 검증 판정 기준을 재정의하지 않음을 확정한다(§5).

이 규격은 07 §3.1-C·§3.2-D 계약의 **인스턴스**다. 연산·데이터 계약·불변 규칙을 재정의·확장하지 않는다. 형태 A(문서)에서 형태 B(실행 코드)로 전환되어도 07 §3 Core Contract 변경은 0이며, 위반(형태 B가 07 §3 계약을 바꾸어야만 성립하는 경우)이 발견되면 구현하지 않고 Advisor에게 보고한다(structure.md §7 C-1과 같은 불변 원칙).

---

## §2. Merge 연산 인터페이스 인스턴스 (정본: 07 §3.1-C)

Merge는 완료·검증된 Task 결과를 수합·정합성 검증·충돌 처리하는 연산이다(07 §3.1-C). 아래는 그 연산의 입력·출력·완료 조건·실패를 07 §3.1-C 정본과 일치하게 옮긴 인스턴스이며, 진위 판정 기준은 07 §3.1-C가 유지한다(§0). 본 절은 계약을 재정의하지 않는다(재정의·확장 0).

| 요소 | 값 (정본: 07 §3.1-C) |
|---|---|
| **입력** | 완료·검증된 Task 결과 집합 — 각 결과는 완료 보고(02 §3.2-C)를 동반한다. |
| **출력** | 병합 결과 + 상호 참조 정합성 판정 + (있다면) 충돌 목록. (출력의 포맷 인스턴스는 §3 Merge Result다.) |
| **완료 조건** | (1) 모든 Task 결과가 개별 검증을 통과했다(INV-5). (2) Task 간 상호 참조가 정합하다. (3) 충돌이 0건이거나, 발견된 충돌이 Advisor 중재로 해소되었다(INV-6). |
| **실패 보고 (reason 3종)** | `UnverifiedResult` \| `CrossReferenceMismatch` \| `UnresolvedConflict`. |

- **완료 보고 = 입력 동반물(참고 입력), 판정 근거 아님.** 각 Task 결과는 완료 보고(02 §3.2-C — `artifacts`/`self_check`/`failures`/`open_questions`/`verify_basis`, § 포인터·재정의 0)를 동반한다(07 §3.1-C 입력). 완료 보고의 5필드 정본은 02 §3.2-C가 소유하며 본 문서는 인용만 한다. Merge는 이 완료 보고를 동반물로 받되, **개별 검증 통과 여부의 판정 근거로 삼지 않는다** — 개별 검증은 Verifier 독립 판정(06 §3.1, CP2)의 산출이며 완료 보고의 `self_check`는 검사 대상(claim)이다(06 V1). Merge는 그 독립 판정의 최종 판정(Pass)을 선행 조건으로 요구할 뿐이다(§5).
- **개별 검증은 Merge가 수행하지 않는다.** "개별 검증을 통과했다"의 판정 기준은 06 §3.2-C 소관이며, Merge는 검증 통과 여부를 병합의 선행 조건으로 요구할 뿐 판정 자체를 정의하지 않는다(07 §3.1-C 주의, INV-8·Non-Goals). 본 표의 완료 조건 (1)은 그 선행 조건을 소비하는 지점이다(§5).
- **실패 보고 포맷.** Merge 실패 시 위 3종 `reason`은 공통 Failure Report(07 §3.2-E) 구조로 보고된다. 그 4필드 구조(`operation`/`target`/`reason`/`location`)와 9종 `reason` 전체 열거의 **포맷 인스턴스는 work-graph.md §4가 소유**하며, 본 문서는 재게재하지 않고 § 포인터로 소비한다. Merge 실패의 `operation` 값은 `Merge`이고, 본 문서가 소유하는 것은 위 3종 `reason`이 **어느 완료 조건 미충족에서 산출되는가의 결합**이다(§4 결합표).
- 입력·출력·완료 조건·실패의 상세 정본은 07 §3.1-C가 유지한다. 본 표는 그 연산을 인스턴스화할 뿐 계약을 재정의하지 않는다.

---

## §3. Merge Result 포맷 인스턴스 (정본: 07 §3.2-D)

Merge Result는 병렬 결과의 수합·정합성 검증·충돌 처리 결과를 담는다(07 §3.2-D). 아래 필드·의미는 07 §3.2-D 정본을 **그대로 보존**한다(재정의 0, 새 필드 0, 새 열거 0). 정본 표는 필수 열을 두지 않으므로 본 인스턴스도 두지 않는다(정본 구조 보존).

| 필드 | 의미 |
|---|---|
| `collected` | 수합된 Task 결과 집합. 각 항목은 개별 검증을 통과한 결과만 포함한다 (INV-5). |
| `crossRefStatus` | 상호 참조 정합성 판정 — `Consistent` / `Mismatch`. 한 Task가 참조하는 다른 Task의 계약·산출물이 실제와 일치하는지. |
| `conflicts` | 충돌 목록. 두 Task가 같은 계약을 다르게 실현했거나 소유 경계가 사후 충돌한 경우. 각 항목은 중재 대상이다. |
| `arbitration` | 충돌 해소 결과. 중재자는 Advisor다 (INV-6). Workflow는 충돌을 검출·보고하고, 해소 결정은 Advisor가 내린다. |

- **`crossRefStatus` 열거(정본 보존).** `crossRefStatus`는 `Consistent` / `Mismatch` 두 값이 전부다(07 §3.2-D). 새 값을 추가하지 않는다. 이 값은 §4 단계 2(상호 참조 정합성 검증)의 검사 결과로 산출된다.
- **`arbitration` 중재자 = Advisor(정본 보존, INV-6).** 충돌의 해소 결정은 Advisor가 내린다. Workflow(이 규격)는 충돌을 **검출·보고**하고, 해소 **결정**은 Advisor 소관이다(07 §3.2-D, INV-6, 02 §3.2-A 역할 경계). 본 문서는 이 중재자 지정을 재정의하지 않는다(§5).
- **참조 대상 포맷의 소관.** `crossRefStatus`가 대조하는 "다른 Task의 계약·산출물"의 계약은 Task의 확정된 인터페이스 계약(`interfaceContract`)이고, `conflicts`가 참조하는 소유 경계는 `ownedBoundary`다 — 두 필드의 포맷 정본은 07 §3.2-B이며 그 인스턴스 소유는 work-graph.md §3이다. 본 문서는 그 필드를 재게재하지 않고 § 포인터로만 참조한다.
- 필드의 상세 정본은 07 §3.2-D가 유지한다. 본 표는 그 스키마를 인스턴스화할 뿐 계약을 재정의하지 않는다.

---

## §4. 충돌 처리 순서 → 검사·처리 규칙 전개 (정본: 07 §3.2-D 순서 문면)

07 §3.2-D 정본은 충돌 처리 순서를 다음 문면으로 규정한다(정본 보존):

> 수합 → 상호 참조 정합성 검증 → 충돌 검출 → (충돌 시) Advisor 중재 → 병합 성립.

아래는 이 순서 문면을 보존하고, 각 단계를 **예/아니오 판정 가능한 검사·처리 규칙**으로 전개한 것이다. 전개는 07 §3.1-C 완료 조건·§3.2-D 필드·§6 실패 시나리오에 결합할 뿐 계약을 재정의·확장하지 않는다. 특히 **개별 검증의 판정 기준(무엇이 통과인가)은 전개하지 않는다** — 06 §3.2-C 소관이며 §5가 § 포인터로만 참조한다.

### §4.1 단계 1 — 수합 (Collect) → 산출: `collected`

- **검사(예/아니오).** 각 Task 결과에 대해: (a) 완료 보고(02 §3.2-C)를 동반하는가? **그리고** (b) 그 결과가 개별 검증을 통과했는가 — 즉 그 결과에 대한 독립 판정의 최종 판정이 Pass인가(INV-5; 판정 기준은 06 §3.2-C 소관, §5 포인터)?
- **처리.**
  - (a)·(b) **모두 예** → 그 결과를 `collected`에 포함한다. `collected`의 각 항목은 개별 검증을 통과한 결과만 포함한다(07 §3.2-D, INV-5).
  - (b)가 **아니오**(미검증·최종 판정이 Pass 아님) → 그 결과는 병합 대상이 아니다(INV-5). 미검증 결과를 병합하려는 시도는 완료 조건 (1) 미충족으로 Merge 실패이며 `reason` = `UnverifiedResult`이다(07 §3.1-C, §6).
- **경계.** Merge는 이 단계에서 검증을 **재수행하지 않는다**. 완료 보고는 참고 입력(동반물)이고, 개별 검증 통과 여부는 Verifier 독립 판정(06)의 최종 판정으로 주어진다(§2·§5). 이 단계는 그 최종 판정(Pass)을 선행 조건으로 확인·수합할 뿐이다.

### §4.2 단계 2 — 상호 참조 정합성 검증 (Cross-reference consistency) → 산출: `crossRefStatus`

- **검사(예/아니오).** `collected`의 각 Task가 참조하는 다른 Task의 계약·산출물(확정된 `interfaceContract`·산출물 — 07 §3.2-B·work-graph.md §3 포인터)이 **실제와 일치하는가**(07 §3.2-D `crossRefStatus` 의미)?
- **처리.**
  - **전부 일치** → `crossRefStatus` = `Consistent`. 완료 조건 (2)를 충족하고 다음 단계(충돌 검출)로 진행한다.
  - **하나라도 불일치** → `crossRefStatus` = `Mismatch`. 완료 조건 (2) 미충족으로 Merge 실패이며 `reason` = `CrossReferenceMismatch`이다. 이 불일치는 Advisor 중재로 넘긴다(07 §6 — 상호 참조 불일치 → Advisor 중재). 해소 결정은 Advisor 소관이다(INV-6).
- **경계.** 상호 참조 정합성은 **Workflow 자기 소관**의 판정이며(07 §3.1-C 출력 "상호 참조 정합성 판정", §3.2-D `crossRefStatus`), 개별 Task의 검증 판정 기준(06 소관, §5)과 **구분된다**. 이 단계는 Task 간 참조의 실제 일치 여부만 판정하며, 각 Task가 개별적으로 검증을 통과했는가는 단계 1이 다룬다.

### §4.3 단계 3 — 충돌 검출 (Conflict detection) → 산출: `conflicts`

- **검사(예/아니오).** 두 Task가 **같은 계약을 다르게 실현**했는가, 또는 **소유 경계(`ownedBoundary`)가 사후 충돌**하는가(07 §3.2-D `conflicts` 의미)?
- **처리.**
  - **그런 사례가 있으면** → 각 사례를 `conflicts` 목록에 추가한다. `conflicts`의 각 항목은 중재 대상이다(07 §3.2-D). 다음 단계(Advisor 중재)로 넘긴다.
  - **없으면** → `conflicts` = 0건. 완료 조건 (3)의 앞 분기(충돌 0건)를 충족한다.
- **경계.** Workflow는 이 단계에서 충돌을 **검출·보고만** 한다. 어느 실현을 채택할지의 **해소 결정은 내리지 않는다** — 그것은 단계 4의 Advisor 중재 소관이다(INV-6). 소유 경계의 사전 비중첩은 분해 시 보장되며(07 INV-2, Decompose 소관 — 비소관 포인터), 이 단계는 병합 시점의 **사후** 충돌만 검출한다.

### §4.4 단계 4 — (충돌 시) Advisor 중재 (Arbitration) → 산출: `arbitration`

- **검사(예/아니오).** `conflicts` 목록이 **0건 초과**인가(단계 2의 `Mismatch` 및 단계 3의 검출 충돌 포함)?
- **처리.**
  - **예(1건 이상)** → Advisor 중재로 넘긴다. Workflow는 충돌을 검출·보고하고, **해소 결정은 Advisor가 내린다**(INV-6, 02 §3.2-A — 설계·역할 충돌의 결정 권한은 Advisor). 각 충돌이 Advisor 중재로 해소되면 그 결과가 `arbitration`에 담기고 완료 조건 (3)의 뒤 분기(중재 해소)를 충족한다. 발견된 충돌이 **미해소로 남으면** 완료 조건 (3) 미충족으로 Merge 실패이며 `reason` = `UnresolvedConflict`이다(07 §3.1-C, §6 — Advisor가 중재한다).
  - **아니오(0건)** → 이 단계를 건너뛴다(`arbitration` 불필요).
- **경계.** 이 규격은 중재 **결정 권한을 갖지 않는다**(INV-6). 충돌의 검출·보고·중재 진입 라우팅까지가 Workflow 소관이고, 어떤 실현을 채택·기각할지의 재량 결정은 Advisor 소관이다(02 §3.2-A). 중재 진입점의 **물리 채널**은 이 문서가 서술하지 않는다 — Adapter Binding 문서 소관이다(07 §4.1 충돌 중재 행·§4.2 중재 진입점, §6).

### §4.5 단계 5 — 병합 성립 (Merge established)

- **검사(예/아니오).** 완료 조건 3건이 **모두 충족**되는가? — (1) `collected`의 모든 결과가 개별 검증 통과(INV-5), (2) `crossRefStatus` = `Consistent`, (3) `conflicts` = 0건이거나 발견된 전 충돌이 Advisor 중재로 해소(INV-6).
- **처리.**
  - **셋 다 예** → 병합이 성립한다. 출력은 병합 결과 + 상호 참조 정합성 판정 + (있다면) 충돌 목록이다(07 §3.1-C 출력, §3 Merge Result).
  - **하나라도 아니오** → 병합은 성립하지 않는다(완료가 아니다 — INV-5·ARCHITECTURE 3.4 정합). 미충족 조건에 대응하는 `reason`으로 Merge 실패를 보고한다(공통 Failure Report 포맷 — work-graph.md §4 포인터).

### §4.6 단계 ↔ Merge Result 필드 ↔ 완료 조건 ↔ 실패 reason 결합표 (done 대상 — 1:1 결합)

아래 표가 done의 "각 단계가 예/아니오 판정 가능한 검사·처리 규칙으로 전개되고, 각 `reason`을 어느 완료 조건 미충족에서 산출하는가의 결합"의 대조 지점이다(work-graph.md §0·§4가 규칙 인스턴스 문서 소관으로 지목한 결합).

| 순서 단계 | 검사 (예/아니오) | 산출 필드 (07 §3.2-D) | 대응 완료 조건 (07 §3.1-C) | 미충족 시 `reason` (07 §3.1-C) |
|---|---|---|---|---|
| **1. 수합** | 각 결과에 완료 보고 동반 + 개별 검증 통과(= 06 §3.2-C 최종 판정 Pass)? | `collected` | (1) 모든 결과 개별 검증 통과 (INV-5) | `UnverifiedResult` |
| **2. 상호 참조 정합성 검증** | 각 Task의 상호 참조가 실제와 일치? | `crossRefStatus` (`Consistent`/`Mismatch`) | (2) Task 간 상호 참조 정합 | `CrossReferenceMismatch` (→ Advisor 중재, 07 §6) |
| **3. 충돌 검출** | 같은 계약 상이 실현 or 소유 경계 사후 충돌? | `conflicts` | (3) 앞 분기: 충돌 0건 | (검출만 — `reason`은 4단계 미해소 시 산출) |
| **4. (충돌 시) Advisor 중재** | `conflicts` > 0? → Advisor가 해소? | `arbitration` | (3) 뒤 분기: Advisor 중재 해소 (INV-6) | `UnresolvedConflict` (미해소 시) |
| **5. 병합 성립** | 완료 조건 (1)(2)(3) 전부 충족? | (출력 전체 — §3) | (1)·(2)·(3) 전부 | 미충족 조건에 대응하는 `reason` |

- 이 결합표는 07 §6 대표 실패 시나리오와 정합한다 — 미검증 결과 병합 → `UnverifiedResult`(단계 1), 상호 참조 불일치 → `CrossReferenceMismatch` → Advisor 중재(단계 2), 충돌 미해소 → `UnresolvedConflict` → Advisor 중재(단계 4).
- 각 단계의 **진위 판정 기준의 정본**은 07 §3.1-C(완료 조건·실패)·§3.2-D(필드·순서)가 유지한다. 본 표는 순서 문면을 검사·처리 규칙으로 전개하고 결합을 확정할 뿐 계약을 재정의하지 않는다.

---

## §5. INV-5·INV-6 준수 및 06 §3.2-C 판정 기준 참조 경계

Merge 연산이 준수하는 두 불변과, 검증 판정 기준을 06 §3.2-C에 § 포인터로만 위임하는 경계를 명문화한다. 검증 대조 지점이다.

### §5.1 INV-5 (검증 후 병합) — 판정 기준은 06 §3.2-C 참조 (재정의 0)

- **불변(07 §3.3 INV-5 보존).** 각 Task 결과는 개별 검증을 통과한 뒤에만 병합 대상이 된다. 검증 판정 기준은 이 규격이 정의하지 않는다(06-verifier 소관). 검증되지 않은 결과의 병합은 완료가 아니다(ARCHITECTURE.md 3.4).
- **"개별 검증을 통과했다" = 06 §3.2-C 최종 판정 Pass(§ 포인터 전용).** 그 판정 기준은 06 §3.2-C 최종 판정(통과/실패/조건부 도출 규칙)이 소유하며, "통과했다"는 최종 판정이 **Pass**인 상태를 뜻한다. 본 문서는 이를 07 §9 결정 기록("INV-5의 판정 기준은 06 §3.2-C 최종 판정 사용")과 정합하게 **§ 포인터로만 참조**하고, Pass/Fail/Conditional 도출 규칙을 **재서술하지 않는다**(판정 기준 재정의 0). 조건부(Conditional)·실패(Fail)는 Pass가 아니므로 개별 검증 통과가 아니며, 그 결과는 §4.1 단계 1에서 `collected`에 포함되지 않는다.
- **검증 리포트 스키마는 소비 지점 포인터만.** 개별 검증의 산출물인 검증 리포트의 스키마는 06 §3.2-A 정본이며 그 인스턴스는 framework/verifier/verification-report.md가 소유한다. 본 문서는 그 스키마를 재게재하지 않고 § 포인터로만 소비한다 — 병합 선행 조건이 요구하는 것은 그 리포트의 **최종 판정(Pass)** 결과이며, 리포트의 필드·구조는 06·verification-report.md 소관이다.
- **Merge는 검증을 수행하지 않는다.** 개별 검증(무엇이 통과인가의 판정)은 Verifier 독립 판정(06 §3.1, CP2) 소관이고, Merge는 그 최종 판정(Pass)을 병합의 **선행 조건**으로 소비할 뿐이다(07 §3.1-C 주의, INV-8·Non-Goals). 이 경계 위반(Merge가 검증 판정 기준을 정의해야만 성립하는 경우)이 발견되면 구현하지 않고 Advisor에게 보고한다.

### §5.2 INV-6 (중재자 = Advisor) — 검출·보고와 결정의 분리 (재정의 0)

- **불변(07 §3.3 INV-6 보존).** 충돌의 중재자는 Advisor다. Workflow는 충돌을 검출·보고하고, 해소 결정은 Advisor가 내린다. 설계·역할 충돌의 결정 권한은 Advisor에게 있다(02 §3.2-A 역할 경계).
- **검출·보고 = Workflow(이 규격) 소관 / 해소 결정 = Advisor 소관.** §4의 단계 2(상호 참조 불일치)·단계 3(충돌 검출)까지가 Workflow의 검출·보고이고, 단계 4의 **해소 결정**은 Advisor가 내린다. `arbitration`(§3)은 그 Advisor 결정의 결과를 담는다. 본 규격은 어떤 실현을 채택·기각할지의 재량 결정 권한을 갖지 않는다.
- **미해소 충돌의 귀결.** 발견된 충돌이 Advisor 중재로 해소되지 않으면 완료 조건 (3) 미충족으로 Merge 실패(`UnresolvedConflict`)이며, 병합은 성립하지 않는다(§4.4·§4.5). 재시도·재분해·재위임 등 후속 흐름의 오케스트레이션은 이 포맷 규격 밖이며 상위 오케스트레이션 소관이다(추측하지 않고 포인터만 — §6).

---

## §6. 경계와 비의존 (재정의 0 · 비소관 · 07 R2 · 금지 토큰 · Glossary)

본 문서가 준수하는 경계를 한자리에 모은다. 검증 대조 지점이다.

- **07 계약 재정의·확장 0.** §2~§4의 모든 연산 요소·Merge Result 필드·순서 단계는 07 §3.1-C·§3.2-D의 인스턴스이며 § 포인터로만 참조한다. 새 필드·새 열거·새 `reason`·새 완료 조건을 신설하지 않았다. §2 연산 표는 07 §3.1-C 정본을, §3 Merge Result 표는 07 §3.2-D 정본을(필수 열 부재 구조 포함) 그대로 보존했다. 위반(형태 B가 07 §3 계약을 바꾸어야만 성립하는 경우)이 발견되면 구현하지 않고 Advisor에게 보고한다.
- **인스턴스화 대상 경계 — Merge만, Decompose·Dispatch 비소관.** Decompose(07 §3.1-A)·Dispatch(07 §3.1-B) 연산의 전개는 이 파일 소관이 아니다 — 각각 별도 인스턴스 문서 소관이며, 본 문서는 "07 §3.1-A/§3.1-B 소관" 수준의 일반 포인터로만 가리킨다. 소유 경계 사전 비중첩(07 INV-2, 분해 시 보장)도 Decompose 소관이며 본 문서는 병합 시점의 사후 충돌만 다룬다(§4.3).
- **검증 판정 기준 비정의(07 INV-8·Non-Goals).** "무엇이 개별 검증 통과인가"는 정의하지 않는다 — 06 §3.2-C 소관이며 최종 판정 Pass를 § 포인터로만 소비한다(§5.1). 상호 참조 정합성 판정(`crossRefStatus`)은 이와 구분되는 Workflow 자기 소관이며, 06의 검증 판정 기준을 침범하지 않는다(§4.2).
- **공통 Failure Report·Work Graph·Task 포맷 소비 경로(재게재 0).** 공통 Failure Report 4필드(`operation`/`target`/`reason`/`location`)와 9종 `reason` 전체 열거의 포맷 인스턴스는 work-graph.md §4가 소유하고, Work Graph(07 §3.2-A)·Task(07 §3.2-B, `interfaceContract`·`ownedBoundary` 포함)의 포맷 인스턴스는 work-graph.md §2·§3이 소유한다. 본 문서는 이 포맷들을 **재게재하지 않고** 저장소 문서 식별자 + § 포인터로만 소비하며, Merge의 3종 `reason`이 어느 완료 조건 미충족에서 산출되는가의 **결합만** 소유한다(§4.6).
- **위임 계약·완료 보고 인용 경계(07 INV-3).** 완료 보고(02 §3.2-C)·역할 경계(02 §3.2-A)는 02 소유이며 § 포인터로만 인용했다. 위임·역할 경계 포맷을 재정의하지 않았다.
- **07 R2 경계 — 동시 작성 형제 불인용.** 같은 Wave(v0.7)에서 동시 작성 중인 형제 산출물(분해 규칙·디스패치 프로토콜 인스턴스)의 미완성 내용은 인용·추측하지 않았다. 확정된 정본(07·06·02·structure.md·Glossary)과 확정 형제(work-graph.md WF1 확정본, verification-report.md)만 참조했다. Decompose·Dispatch 결합은 "07 §3.1-A/§3.1-B 소관" 수준의 일반 포인터로만 지시했다.
- **직렬화·물리 채널 비서술.** 병합 결과 수합·상호 참조 대조·충돌 중재 진입점의 물리 채널·직렬화·물리 위치는 서술하지 않고 **Adapter Binding 문서 소관**(07 §4.1 상호 참조 정합성·충돌 중재 행·§4.2 중재 진입점) 포인터로만 처리했다. 특정 Adapter Binding 문서명·물리 진입점 토큰을 두지 않았고, 필요한 자리에는 "Adapter Binding 문서 소관(07 §4.1·§4.2)" 포인터만 두었다.
- **금지 토큰 비의존(structure.md §5 C-3 확장, 07 INV-9) — 자가 전수 스캔 수행.** 본문 전체를 다음 후보 부류 **전체**로 전수 스캔하여 실증 0건임을 확인했다(단일 토큰 검색 하나로 넓은 결론을 내지 않고, 부류별 전수 대조) — { 특정 AI 이름·모델명·제품 기능명 } ∪ { 프로그래밍 언어명·툴체인명 } ∪ { 직렬화 형식명·확장자 } ∪ { 물리 경로·물리 진입점 토큰·특정 Adapter Binding 문서명 }. 물리 실현이 필요한 자리에는 구체 토큰 대신 "Adapter Binding 문서 소관(07 §4.1·§4.2)" 포인터를 두었다 — 금지 토큰의 예시조차 본문에 나열하지 않았다(mention/use 경계). 다음은 금지 토큰이 아니다: **(i)** Merge Result 필드 값·연산 열거 명칭 — `operation` 값(`Merge`), `crossRefStatus` 값(`Consistent`/`Mismatch`), `reason` 3종(`UnverifiedResult`/`CrossReferenceMismatch`/`UnresolvedConflict`) — 07 §3.1-C·§3.2-D 정본의 평이한 열거 값; **(ii)** 06 최종 판정 열거(통과/실패/조건부, Pass/Fail/Conditional) — 06 §3.2-C 정본 어휘; **(iii)** Glossary·AGENT.md 정본 어휘 — Work Graph·Task·병렬 집합·소유 경계·인터페이스 계약(Glossary §3.2-J J-07)과 역할 명칭(Advisor/Worker/Verifier); **(iv)** 계약 필드명 백틱 표기(`collected`·`conflicts`·`interfaceContract`·`ownedBoundary` 등, 07 §3.2 정본 어휘); **(v)** 저장소 문서 식별자(`specs/…`·`framework/…`·`docs/…` 상호 참조 및 본 문서 자신의 식별자 `framework/workflow/merge-rules.md`) — 문서 식별자이며 직렬화 형식·물리 경로 토큰이 아니다(work-graph.md §5·verification-report.md §7·loop-protocol.md §7 분류 선례 동형).
- **Glossary 정본.** 사용 용어는 전부 specs/00-glossary.md §3.2-J(J-07) 정본 또는 07 §3.1-C·§3.2-D 정본이 정의한 연산·포맷 명칭이거나 평이한 열거 값이다. 새 용어를 신설하지 않았다.

---

## §7. 요약 (규격 한눈에 보기)

- 이 문서 = **Merge 연산(07 §3.1-C)·Merge Result(07 §3.2-D)·충돌 처리 순서의 규칙 인스턴스**. 정본 = 07 §3.1-C·§3.2-D(본 문서는 인스턴스, 재정의 아님 — C-1). work-graph.md §0이 "07 §3.2-D 소관 별도 인스턴스 문서(병합 규칙)"로 지목한 자리.
- **Merge 연산(§2)** — 입력=완료·검증된 Task 결과 집합(완료 보고 02 §3.2-C 동반, 참고 입력), 출력=병합 결과+상호 참조 정합성 판정+(있다면) 충돌 목록, 완료 조건 3건(개별 검증 통과 INV-5 / 상호 참조 정합 / 충돌 0 or Advisor 중재 해소 INV-6), 실패 `reason` 3종. 정본 보존, Merge는 검증을 수행하지 않는다.
- **Merge Result(§3)** — 4필드(`collected`/`crossRefStatus`/`conflicts`/`arbitration`). 07 §3.2-D 인스턴스, `crossRefStatus` 열거(Consistent/Mismatch)·`arbitration` 중재자=Advisor(INV-6) 정본 보존. 새 필드·새 열거 0.
- **충돌 처리 규칙(§4)** — 순서(수합→상호 참조 정합성 검증→충돌 검출→(충돌 시) Advisor 중재→병합 성립) 보존 + 5단계를 예/아니오 검사·처리 규칙으로 전개. §4.6 결합표: 단계 ↔ Merge Result 필드 ↔ 완료 조건 ↔ 실패 `reason` 1:1 결합(07 §6 정합).
- **불변·판정 기준 경계(§5)** — INV-5(검증 후 병합): 개별 검증 통과=06 §3.2-C 최종 판정 Pass(§ 포인터 전용, 도출 규칙 재서술 0, 07 §9 결정 기록 정합). INV-6(중재자=Advisor): 검출·보고=Workflow, 해소 결정=Advisor. 검증 리포트 스키마는 06 §3.2-A·verification-report.md 소비 포인터.
- **경계(§6)** — 07 재정의·확장 0, Merge만 소관(Decompose·Dispatch 비소관), 검증 판정 기준 비정의(07 INV-8 — 06 소관), 공통 Failure Report·Work Graph·Task 포맷은 work-graph.md 포인터 소비(재게재 0), 07 R2 동시 작성 형제 불인용, 금지 토큰 0(자가 전수 스캔), Glossary J-07 정본만 사용.
- 모든 규칙은 07 §3.1-C·§3.2-D의 인스턴스이며, 물리 실현(수합·중재 진입점 채널)은 Adapter Binding 문서 소관이다(07 §4.1·§4.2). 형태 A(문서) → 형태 B(실행 코드) 전환에도 Core Contract 변경은 0이다.
