# [시연 픽스처 — 실계약 문서 아님] fixture-t2-spec — 3-Task 병렬 집합 T2 (consumes 계약 X · produces 계약 Y)

> **시연 픽스처 — 실계약 문서 아님.** 이 파일은 v0.7 Workflow 시연(Task WF9, 3-Task 병렬 집합의 T2 — consumes 계약 X · produces 계약 Y)을 위해 구성된 **자기완결 대상 명세**다. specs/·framework/ 등 실계약 문서가 **아니다**. 이 파일과 그 산출물(out-t2.md)은 `docs/v0.7-demo-fixtures/` **내부에만** 존재한다 (v0.7-demo-procedure §4.1 격리, CP2 전수 스캔 제외 verifier_scope).
>
> **의도적 결함 정당 보유 (T2 충돌 유도 지점 — §4.3).** 본 spec의 Y 실현 지시(§A — 계약 X 등장 순 **베타 → 알파 → 감마**)는 확정 계약 Y 문면(가나다 순 **감마 → 베타 → 알파**)과 **의도적으로 불일치**한다 — **Merge 단계 3(충돌 검출) 시연 유도 지점**이다. 이 불일치는 실계약 결함이 아니라 Workflow 병합·충돌 처리 실증을 위한 **의도적·정당 보유물**이다. 단 out-t2.md는 **T2 자기 done(§B)을 충족**하므로 개별 CP2는 **Pass**다 — T3가 참조하는 확정 Y 문면과의 사후 불일치는 개별 검증이 아니라 **Merge가 검출**한다.

---

## §A. 대상물 · 산출 위치 (위임 대상 작업 — consumes X · produces Y, 실현 A)

계약 X(out-t1.md)의 용어·정의를 소비하여 **용어 색인 표**를 작성하라. out-t2.md는 `| 용어 | 정의 요약 |` **2열 표**로 용어 3개를 **계약 X 등장 순서(베타 → 알파 → 감마)**로 나열하고, 각 정의 요약은 계약 X의 `- 정의:` 줄 **요지**를 담는다(실현 A).

- **산출 위치:** `docs/v0.7-demo-fixtures/out-t2.md` (후속 Task Worker가 생성).
- **참조 경계 (R2).** 확정된 계약 X 문면만 참조한다 — 동시 진행 중인 형제 Task의 미완성 산출물은 추측·인용하지 않는다.

## 계약 문면 (consumes: 계약 X 인용 / produces: 계약 Y 실현 지시)

> **소비 — 계약 X (인용).** out-t1.md는 `## 용어: 베타` → `## 용어: 알파` → `## 용어: 감마` 3개 헤더를 이 등장 순서대로 갖고, 각 헤더 바로 아래 `- 정의:` 로 시작하는 줄을 정확히 1개씩 둔다.
>
> **산출 — 계약 Y (실현 A · 본 spec의 실현 지시).** out-t2.md는 `| 용어 | 정의 요약 |` 2열 표로 용어 3개를 **계약 X 등장 순서(베타 → 알파 → 감마)**로 나열한다.

## §B. 완료 조건 (done — 전 항목 예/아니오 판정 가능)

① out-t2.md에 `| 용어 | 정의 요약 |` **2열 표**가 존재한다.
② 표의 데이터 행 3개가 **베타 → 알파 → 감마 순서**다.
③ 각 행의 정의 요약이 계약 X의 `- 정의:` 줄 요지에 대응한다.
④ 산출 위치가 `docs/v0.7-demo-fixtures/out-t2.md`다.
