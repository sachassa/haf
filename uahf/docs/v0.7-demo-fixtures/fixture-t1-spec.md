# [시연 픽스처 — 실계약 문서 아님] fixture-t1-spec — 3-Task 병렬 집합 T1 (produces 계약 X)

> **시연 픽스처 — 실계약 문서 아님.** 이 파일은 v0.7 Workflow 시연(Task WF9, 3-Task 병렬 집합의 T1 — produces 계약 X)을 위해 구성된 **자기완결 대상 명세**다. specs/·framework/ 등 실계약 문서가 **아니다**. 이 파일과 그 산출물(out-t1.md)은 `docs/v0.7-demo-fixtures/` **내부에만** 존재한다 (v0.7-demo-procedure §4.1 격리, CP2 전수 스캔 제외 verifier_scope).
>
> **의도적 결함 정당 보유 — 본 파일 해당 없음.** 본 시연 픽스처 세트의 의도적 결함(정당 보유)은 충돌 유도 지점 1건으로 **fixture-t2-spec에만** 심겨 있다(§4.3 — Merge 단계 3 충돌 검출 유도). **T1 자기 파일에 심긴 결함은 없다** — 결함 없는 **통과 케이스**로, §A 작성 지시를 충실히 따른 **첫 산출로 §B done 전 항목이 충족 가능**하다(개별 CP2 Pass 설계).

---

## §A. 대상물 · 산출 위치 (위임 대상 작업 — produces 계약 X)

용어 3개(베타·알파·감마)의 **정의 문서**를 작성하라. 각 용어를 `## 용어: <이름>` 헤더로 두고, 헤더 **바로 아래**에 그 용어의 정의를 `- 정의:` 로 시작하는 줄 **정확히 1개**로 적는다. 헤더는 **베타 → 알파 → 감마 등장 순서**로 나열한다(계약 X 실현).

- **산출 위치:** `docs/v0.7-demo-fixtures/out-t1.md` (이 파일은 본 spec이 아니라 후속 Task Worker가 생성한다).

## 계약 문면 (produces: 계약 X — 분해 시점 확정)

> **계약 X.** out-t1.md는 `## 용어: 베타` → `## 용어: 알파` → `## 용어: 감마` 3개 헤더를 **이 등장 순서대로** 갖고, 각 헤더 바로 아래 `- 정의:` 로 시작하는 줄을 정확히 1개씩 둔다.

## §B. 완료 조건 (done — 전 항목 예/아니오 판정 가능)

① out-t1.md에 `## 용어: 베타`·`## 용어: 알파`·`## 용어: 감마` 3개 헤더가 **베타 → 알파 → 감마 등장 순서**로 존재한다.
② 각 헤더 바로 아래에 `- 정의:` 로 시작하는 줄이 **정확히 1개**씩 있다.
③ 산출 위치가 `docs/v0.7-demo-fixtures/out-t1.md`다.
