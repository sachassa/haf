# Project Contract v1 (테스트 픽스처)

contract_to_graph 테스트용 최소 소비 워크스페이스 픽스처다. resolve_contract 는 파일명
버전만 판독하고 내용을 파싱하지 않는다 — v2 와 병존시켜 "최대 버전 선택(v1 아님)"을 검증한다.
(원 실측 대상이던 tms-system 트랙은 2026-07-19 종료·삭제 — 앵커 = ARCHIVE.md 원장.)

커버리지 백스톱 통과 근거는 같은 디렉터리의 `coverage-ledger.json` 이다(v1·v2 공유).
