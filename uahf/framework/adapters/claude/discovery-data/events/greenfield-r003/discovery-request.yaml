# Discovery Request (Entry Resolution 산출 — 구조화 레코드)
# 직렬화 형식 정본 = entry-binding.md §5.1 / 기록 위치 정본 = discovery-binding.md §4.2 (DP-X8)
# Entry Resolution 실수행 기록: 2026-07-13, 주 세션(Advisor) — uaf-new 발화(논리 Entry /new; 사용자 지시
#   "신규 프로젝트를 생성한다" — UAHF Control Plane/Dashboard 트랙, 플랜 승인 2026-07-13)
# 대상 워크스페이스: C:\my-claude-project\<Discovery 확정명>\ — 신규 독립 디렉터리(사용자 지시:
#   프로젝트명·디렉터리명은 Discovery에서 확정). 현 시점 생성 전(미존재) — 실측.
# 엔진 5단계 (01 §3.2-A): ① /new Descriptor 매칭 ② 증거 관측(실측 — entry-binding §4 판정 수단)
#   contract-presence: 대상 워크스페이스 미존재(생성 전) → .claude/project-contract/ 인스턴스 파일 없음 → 무
#   repository-presence: 대상 워크스페이스 미존재(생성 전) → 선재 콘텐츠 무·VCS 마커 무 → 무
# ③ 우선순위 평가: {무, 무} → 01 §3.2-D 행 1 매칭(P-C 순수 Greenfield 정상 경로) ④ 결정성 검증: 단일 행(1)만 매칭 ⑤ 방출: 아래 레코드
mode: greenfield
inputs:
  - sourceType: contract-presence
    observed: 무
  - sourceType: repository-presence
    observed: 무
policy: default-policy   # discovery-data/policy/default-policy.yaml 참조 (Policy as Data)
